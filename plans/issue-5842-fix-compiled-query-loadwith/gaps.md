# Gap ledger: issue-5842-fix-compiled-query-loadwith

Attribution of review findings back to the design that should have prevented them.
Schema: `.claude/docs/work-plan.md`. Append rounds; never replace.

## Round 1 — reviewed HEAD `2aac34ef80`

Plan: `.claude/plans/issue-5842-fix-compiled-query-loadwith/plan.md` (Tier M, `P12` = `refuted` ×2, `P11` empty, all of `P9` recorded `(pending)` except `G-03: n/a`).

**Standing fact that governs most rows below.** `P6`'s authorized files are `CompiledQuery.cs`, `CompiledTable{T}.cs`, `ExpressionQuery.cs`, `ExpressionCacheManager.cs`, `Tests/Linq/Linq/CompileTests.cs`. The branch diff (`6345a3fe5a...2aac34ef80`) changes ten files; six have no `E-n`: `ExposeExpressionVisitor.cs`, `DependentArgumentValues.cs` (new), `ExpressionEvaluator.cs`, `Query.cs`, `Tests/Linq/Extensions/PostgreSQLTests.cs`, `Tests/Linq/Linq/CompileTestsAsync.cs`. `E-4`'s file was never touched at all. The shipped design is not a variant of `E-1…E-5`; it is a different design with no plan artifact, and `P11` records nothing.

### Attribution

- **BLK001** — GAP-08 — `P9`'s own `G-05` row reads `— (pending) Release build on netstandard2.0 / net462 / net10.0`; the gate was applicable, would have caught `ExposeExpressionVisitor.cs:194` verbatim, and was never run — while the PR body asserted its result — G-05 — preventable: yes
- **BLK002** — GAP-09 — `ExposeExpressionVisitor.ExpandOverSource:539` sits in a file with no `E-n` and no `P11` amendment; the amendment owed a `P6` row plus a `P7` row for `EvaluateExpression`'s tree-wide `ParametersParam → Constant` `Transform` and its interaction with `ExpressionCacheManager.cs:106`. `-Reconcile` would have flagged the file, but it runs only on a user-typed `/work-plan check` and has no gate id in `P9`'s `G-01…G-08` table nor a line in `definition-of-done.md`, so no exit path invokes it. Secondary class GAP-05 (no obligation covers a wrapper whose own lambda reads the argument array) — preventable: partly
- **MAJ001** — GAP-09 — same un-authorized function, `ExpandOverSource:537`; an amendment owed a `P4` row on the static types `call.Arguments[0]` can carry (`ITable<T>` vs `IQueryable<T>`), which is the whole finding. `TO-1`/`E-5`'s `WrappedLoadWithTest` covers `.Where(...).LoadWithWrapper(...)` only, so the direct-over-`ITable<T>` form is a named near-miss no obligation reached — preventable: partly
- **MAJ002** — GAP-09 — the `:501` branch is in the same un-authorized file; an amended `P6` row for `TryConvertIQueryable` forces the `P7` question "who else reaches this", whose answer is *every* query's expose path. `P5`'s `D-4` failure-mode line already stated this exact hazard for the abandoned `E-3` ("`CreateQuery` is on the composition path of *every* query"), and the replacement had no block to carry it forward — preventable: partly
- **MIN001** — GAP-09 — `DependentArgumentValues.cs` is a new file with no `E-n` and no `P11`; the `P7` row an amendment owed is the mirrored site `SqlQueryDependentAttribute.ObjectsEqual`, the codebase's existing element-wise comparison for exactly these values (secondary class GAP-03 in shape, but no map existed to miss it) — preventable: partly
- **MIN002** — GAP-09 — `HandleSqlDependentParameters`' new block at `ExposeExpressionVisitor.cs:183-194`, un-authorized; an amendment owed a `P4` row enumerating what `attr.PrepareForCache` can return and which returns preserve reference identity — the `NewArrayInit` rebuild is the case `ReferenceEquals` cannot see (secondary class GAP-02) — preventable: partly
- **MIN003** — GAP-09 — `CollectDependentArgumentIndexes` is new code in `CompiledQuery.cs`, which `E-1` authorizes **by file**: `work-plan.ps1:445` strips the `:Symbol` suffix ("the authorization is per file"), so `E-1`'s row for `CompileQuery` — describing a memoized index-0 node that was never built — passed a wholly different mechanism through `-Reconcile` unflagged. The `P4` row owed: does the pre-expose `[SqlQueryDependent]` argument set equal the during-expose one (secondary class GAP-02, and `P4`'s `OPEN` rule would have forced an answer) — preventable: partly
- **MIN004** — GAP-08 — the falsifiable half is the body's "Release builds of netstandard2.0, net462 and net10.0 are clean", asserted while `P9`'s gate row stood at `(pending)`; the `Quote`-rationale half is prose contradiction with no upstream artifact and would be GAP-10 on its own — G-05 — preventable: yes
- **MIN005** — GAP-07 — the rule exists and is always-loaded (`agent-rules.md` → *"The mirror applies to your own changes: after altering behaviour, grep the prose for statements about it"*), but `/work-plan` step 4 embeds only `.claude/rules/`, KB area files and auto-memory, so no plan ever carries it as an `E-n`/`TO-n` obligation. `E-5` authored that comment truthfully to explain the `[ActiveIssue(5854)]` gate at `4a8addd03f`; un-gating it at `922b08f09c` falsified it and nothing swept — preventable: partly
- **MIN006** — GAP-05 — `P8` carries no `TO-n` for `TwoFoldSitesOfSameTypeDoNotShareQuery`, because `P3` bullet 5 ring-fenced the `table: this` cache key as "already-shipped … not to be revisited". `P8`'s proof-mode rule — `red→green` means red against the unfixed code *for the right reason*, proven by running it — is what would have exposed the old/new key bijection, and it was never required of this test — preventable: partly
- **MIN007** — GAP-05 — same `P3` bullet 5 exclusion (`StartLoadTransaction` named explicitly) removed the change from `P8`'s derivation, so the symmetry obligation `P8` mandates ("where an `E-n` touches a helper reachable from more than one path, one obligation must be a symmetry guard on the unchanged path") was never written for `CompiledTable<T>.ExecuteAsync`. `ElementFormLoadWithOpensLoadTransaction` pins the sync path only — preventable: partly
- **MIN008** — GAP-07 — same missing embed as MIN005: `P8`'s `TO-1` names twice-invocation as load-bearing for *one* obligation, the body generalized it across the eager-loading set, and nothing tied the claim back to the obligations because `P9`'s per-`TO-n` `G-01` rows were never recorded — preventable: partly
- **SUG001** — GAP-10 — assertion strength on a suggestion-severity test for the pre-plan `LinqToDBException` (`537f0520e8`). No plan block governs which substring an assertion checks; this is the "a test whose assertion is weaker than the stated standard" shape that no design document prevents — preventable: no
- **SUG002** — GAP-09 — `Tests/Linq/Extensions/PostgreSQLTests.cs` has no `E-n` and no `P11`, and the whole #5856 fix it tests is absent from a plan scoped to #5842/#5854. The sibling-consistency question ("which four sibling hint tests on this provider group carry `LinqService`?") is the `P7` scout brief's core shape and had no map to appear in — preventable: partly

### Aggregate

`GAP-09` at 7 of 14 is dominant and unambiguous: the answer to *"was the plan wrong, or never amended?"* is **never amended**. The plan's design was correctly refuted and correctly abandoned — `P12` names the mechanism and `P4` U-6 records the probe that killed the alternative — and then three commits shipped a replacement (`ExpandOverSource`, `GetCompiledQueryRootContext`, `HandleSqlDependentParameters`, `DependentArgumentValues`, plus an unmentioned #5856 fix) with **zero** design artifact, no critic pass, and `P11` still reading `_None._`. Corroborating this rather than any finding: the shipped `GetCompiledQueryRootContext` accepts any argument slot holding an `IDataContext` with no `idx == 0` test, which directly contradicts `D-3`'s written decision and its `U-2` evidence — nobody read the plan against the code. On the second question, BLK002 and MAJ001 are a **design** gap, not a test gap: no `TO-n` could exist for a function no block names, and the comment at `ExpandOverSource:449-450` asserting the invariant BLK002 breaks is exactly the shape a critic pass on the delta exists to attack. The single change preventing the most findings is wiring `-Reconcile` into the exit path: it already exists, and run with `-RepoRoot` at the worktree it would have named all six unplanned files, forcing the amendment that carries the other six rows.

### Recommended durable fixes

- `<GAP-09 × 7>` → a `definition-of-done.md` gate (a new plan-conformance item, mapped as `G-09` in `work-plan.md` → *P9 Verification gates*) — add "**Plan conformance**: `work-plan.ps1 -Action reconcile -RepoRoot <worktree>` exits 0, or every unplanned file has a `P11` amendment" to the exit checklist. Today `-Reconcile` runs only from a user-typed `/work-plan check`, has no gate id, and silently reports a clean zero when `-RepoRoot` is left at the primary clone (the script's own comment at `work-plan.ps1:29-33`); note in the item that authorization is per **file** (`work-plan.ps1:445`), so a matched file is not a matched edit.
- `<GAP-09 × 7, second half>` → `work-plan/SKILL.md` → *Mode: amend*, step 3 — re-critique on amendment is currently gated on **Tier L** alone, so this Tier M plan could take a wholesale design replacement after a `refuted` verdict with no critic at all. Key the trigger on magnitude as well as tier: a `P12` verdict of `refuted` followed by resumed implementation, an amendment adding a new file, or one adding more `E-n` rows than it keeps, is a new design and gets a critic pass. Add one line to *Mode: amend* that `P12`'s "the branch ships without this fix" is not a terminal state — resuming work after it is a re-author, not a silent continuation.
- `<GAP-05 × 2>` → `work-plan.md` → `P3` and `P8` semantics — a plan authored **mid-branch** (this one landed after six commits) uses `P3` to ring-fence already-committed work, which removes it from `P8`'s obligation derivation while review still reviews the whole `master...HEAD` diff. Require that an "already shipped on this branch, out of scope" exclusion either names a `TO-n` for the shipped behaviour or states what is therefore unproven — the same discipline `P9` already applies to `skipped`, and the reason MIN006's test cannot fail and MIN007's async path has no guard.

## Round 2 — reviewed HEAD `266c24c4ea`

Plan unchanged since round 1 (Tier M, `P12` = `refuted` ×2, `P11` still `_None._`, all of `P9` still `(pending)` except `G-03: n/a`). Four commits landed since round 1: `03c6c1dd31` and `ffb468b93` (addressing round-1 findings), `de153b5850` (the learn-and-retry cache-key loop — a wholesale new mechanism with no plan row), and `65e21bbd29` (a follow-up fixing a defect that reproduces on `master`). None carries a `P11` amendment.

### Attribution

- **MAJ003** — GAP-09 — `ExposeExpressionVisitor.ExpandOverSource` (`:564-570`) has no `P6` `E-n` and no `P11` amendment — the same file BLK002/MAJ001/MAJ002 flagged in round 1, now carrying more surface. An amendment's `P7` row would have had to ask "who tracks the values this function bakes into the cached tree" against the very safety net this branch built for that purpose (`HandleSqlDependentParameters` → `RecordMaterializedArgumentSlots` → `DependentArgumentValues` → the learn-and-retry loop); `ExpandOverSource` never calls into any of it, so a wrapper argument that is not `[SqlQueryDependent]`-attributed is folded to a literal with zero cache-key coverage. Secondary GAP-02 in shape: the Quote/non-simple-sibling interaction (`EvaluateExpression`'s fast path vs its `Expression.Lambda(...).CompileExpression()` fallback) is an assumption a `P4` row would have named, and none exists because no `P6` row exists to hang it from — preventable: partly
- **MAJ004** — GAP-09 — same un-amended `ExpandOverSource` surface. An amendment's `P7` mirrored-site row would have had to reconcile the function's own comment ("A method the builder does not know") against its literal guard `!call.IsQueryable` — the guard excludes exactly the case the comment describes as included, and the shipped documented workaround for this whole issue is the `[IsQueryable]` opt-in (`Mapping/IsQueryableAttribute.cs`), the mirrored site nothing ever searched — preventable: yes
- **MIN009** — GAP-05 — `P6` `E-3` (`ExpressionQuery.cs:IQueryProvider.CreateQuery` — copy `Parameters` onto the constructed query) is an **authorized** edit-point that was never implemented; confirmed against the shipped file, both overloads still build a bare `ExpressionQueryImpl<TElement>(DataContext, expression)` (`:233-251`) while `Execute<TResult>` does pass `Parameters` (`:262`). `P10`'s final entry names this exact risk verbatim ("`query(db, id).Where(...)` … `TO-2` does not currently assert it … if it does not [work], that is a finding rather than a silent gap"), but no `TO-n` was written to make the omission visible before merge — G-01 — preventable: yes
- **MIN010** — GAP-09 — the learn-and-retry loop (`CompiledTable<T>.GetInfo`/`Union`/`IsCovered`, `de153b5850`) is a wholesale new caching mechanism with no `P6`/`P7`/`P11` row. The constraint it violates — per-invocation argument storage under concurrent use — was already named in `P4` `U-8` ("`ConcurrentTest1/2/3` invoke one compiled query from 100 threads … argument storage must stay per-invocation"), so an amendment citing that unknown would plausibly have caught the unsynchronised `GetOrCreate`-then-`Remove` publish window. Secondary GAP-05: `ConcurrentTest1/2/3`'s identical-argument fixture means no existing obligation can go red regardless — G-01 — preventable: yes
- **MIN011** — GAP-09 — `DependentArgumentValues.cs`, the same unauthorized new file round 1's MIN001 flagged. The `P7` row an amendment owed (mirror `SqlQueryDependentAttribute.ObjectsEqual`, per round 1's own citation) would plausibly have caught the missing `RawSqlString` special case and the string-guard asymmetry on a side-by-side read; the recursion divergence is subtler, because the new code is the *more* correct of the two, so a bare `P7` pass might sign the divergence off as "improved" without catching that the class summary's equivalence claim is now false. Secondary GAP-03, identical in shape to round 1's MIN001 — preventable: partly
- **MIN012** — GAP-05 — `P3` bullet 5's mid-branch exclusion of the already-shipped `StartLoadTransaction` fix still carries no "what is therefore unproven" clause; this is round 1's own recommended durable fix, unapplied (the `P3` text is byte-identical). Had the clause existed, the obvious unproven statement is "sync-path implicit-transaction *disposal* is not witnessed by any test" — precisely `ElementFormLoadWithOpensLoadTransaction`'s gap — G-01 — preventable: partly
- **MIN013** — GAP-05 — same unapplied `P3` clause, and a direct recurrence of round 1's MIN007, which named this exact shape ("`ElementFormLoadWithOpensLoadTransaction` pins the sync path only"). `P8`'s symmetry-guard rule still has no async counterpart, so the differently-written async disposal (`await using var tr = (transaction ?? EmptyIAsyncDisposable.Instance)`) has never had its own witness — G-01 — preventable: partly
- **MIN014** — GAP-05 — same unapplied `P3` clause, and a violation of `P8`'s rule on conditional-path assertions ("the obligation must name what proves it fired"). `ElementFormLoadWithHonorsCancellation`'s `LoadWith`-carrying fixture lets `StartLoadTransactionAsync` absorb the cancellation before `GetElementAsync` — the one site this PR actually fixed — is ever reached, and no obligation names what would prove that site fired — G-01 — preventable: partly
- **SUG003** — GAP-09 — same root as MIN010: the learn-and-retry loop has no `P6`/`P7`/`P8` row, so nothing specified remote (`LinqService`) coverage for it. `CompiledQueryHintFromArgumentTest` was correctly widened to remote in round 1 in response to a coverage finding of this shape (round 1's SUG002), but the widening landed on a test whose dependent position is visible up front and never enters the loop — the letter of the round-1 finding was answered without an amendment naming which test the fix actually needed to land on. Secondary GAP-05 — preventable: partly

### Out-of-scope observations (not round-2 findings — pre-existing on `master`)

- **`ExposeExpressionVisitor.Cleanup()` retains `_parameterValues` across pool reuse.** The plan should have surfaced it: the PR edits this exact method two lines away (`_materializedArgumentSlots = null;`), and `ExposeExpressionVisitor.cs` is the same no-`E-n`/no-`P11` file behind MAJ003/MAJ004/MIN010/MIN011/SUG003. A `P7` row for `Cleanup()` would have had to list every field the method resets and ask why `_parameterValues` alone survives — exactly the "contract a caller must tolerate" search `P7` exists to do, and more pointed now that this branch built caching machinery depending on that field's contents. GAP-03 in class, but pre-existing, so it is evidence the missing `P7` row has now cost twice rather than a finding in its own right.
- **`CompiledTable<T>.Create` / `RuntimeExpressionsContainer` / dynamic-expression accessors.** This one sits inside an **authorized** edit-point (`P6` `E-2`), so the plan should have surfaced it directly: a `P7` row for `Create` asking "who else relies on `Table<T>.Info` / `CompiledExpressions` being built against a single consistent `IQueryExpressions` container" would have named `TableBuilder`'s `FilterFunc` and `AssociationHelper`'s association-predicate dynamic accessors — the mirrored-site search `P7`'s schema mandates and which the actual `P7` never performed for this edit point. GAP-03. The observation is **unprobed** (reachability from a queryable compiled form through a closing-over filter is asserted, not demonstrated), so read it at that strength.

### Aggregate

GAP-09 is again dominant (5 of 9: MAJ003, MAJ004, MIN010, MIN011, SUG003), but GAP-05 — driven entirely by one specific unapplied round-1 recommendation about `P3` mid-branch exclusions — now accounts for 4 of 9 (MIN009, MIN012, MIN013, MIN014). The single change preventing the most round-2 findings is unchanged from round 1: wiring `-Reconcile` into an exit-path gate would have forced a `P11` amendment before the four post-round-1 commits landed, carrying a `P7`/`P8` pass over `ExposeExpressionVisitor.cs`, `DependentArgumentValues.cs` and the new learn-and-retry mechanism — covering MAJ003, MAJ004, MIN010, MIN011 and SUG003 at once. Where round 1 diagnosed *"never amended"*, round 2 shows the same defect compounding: more unauthorized code landed on the same un-amended surface, and a wholesale redesign (the learn-and-retry loop) shipped after a `refuted` critic verdict with zero re-critique — exactly the failure mode round 1's second recommendation was written to close.

### Continuity

Round-1 finding shapes that recur:

- BLK002 / MAJ001 / MAJ002 (`ExposeExpressionVisitor.cs`, GAP-09) → recur directly as MAJ003 and MAJ004: same file, same function, still no `P6`/`P11` row, now with more logic on the same unauthorized surface.
- MIN001 (`DependentArgumentValues.cs`, GAP-09 + secondary GAP-03) → recurs as MIN011: the mirrored-site divergence round 1 predicted materialised as a false equivalence claim in the class's own doc comment.
- MIN003 (`CompiledQuery.cs`, "does the pre-expose `[SqlQueryDependent]` argument set equal the during-expose one?" — never answered by a `P4` row) → its unresolved question is what the learn-and-retry loop patches at runtime; MIN010 is the cost of patching it without a design pass, and MAJ003 shows the patch's coverage is narrower than even that unresolved question implied.
- MIN006 / MIN007 (`P3` bullet 5's exclusion silently removing disposal/symmetry obligations from `P8`) → recur essentially verbatim as MIN012, MIN013 and MIN014.
- SUG002 (remote-coverage map gap, GAP-09) → recurs in shape as SUG003: a widened-test response to a round-1 coverage finding landed on the wrong sibling test.

Round-1 recommended durable fixes — **all three confirmed unapplied**, each with a direct round-2 cost:

1. *Wire `-Reconcile` into the exit path as a `G-09` plan-conformance gate.* **Not applied** — `definition-of-done.md` still lists only `G-01`…`G-08`, and `-Reconcile` still runs only from `/work-plan check`. Would have prevented MAJ003, MAJ004, MIN010, MIN011, SUG003 — five of nine.
2. *Key re-critique-on-amend on magnitude, not tier alone; record `P12`'s "ships without this fix" as non-terminal.* **Not applied** — `work-plan/SKILL.md` → *Mode: amend* step 3 is unchanged and still tier-gated. Second-order cause behind MAJ003/MIN010: the learn-and-retry loop is a wholesale redesign of a `refuted`-verdict plan that shipped with no critic pass at all.
3. *Require a mid-branch `P3` exclusion to name a `TO-n` or state what is therefore unproven.* **Not applied** — `P3` bullet 5 is byte-identical. Would have prevented MIN012, MIN013, MIN014 — three of nine.

No round-1 recommendation addresses MIN009's shape (an *authorized* `P6` edit-point simply never implemented). That is new information this round, not a repeat.

### Recommended durable fixes

- `<GAP-09 × 5>` → `definition-of-done.md` (a `G-09` plan-conformance gate) and `work-plan/SKILL.md` → *Mode: check*. Same recommendation as round 1, now unapplied through a second review round with five more findings behind it. Escalate the wording: note in the item that it has been recommended **twice** without being wired in, so the next reader does not re-derive it as novel.
- `<GAP-05 × 3 (MIN012–014)>` → `work-plan.md` → `P3` semantics. Also a repeat of round 1's third recommendation. Put the "or state what is therefore unproven" clause in the `P3` block's own schema text rather than only in this plan's prose, so `-Validate` can flag a bare "already shipped, not to be revisited" exclusion that carries no such statement.
- `<GAP-05 × 1 (MIN009), new this round>` → `work-plan.md` → `P9` gate derivation, as `-Reconcile`'s mirror image. An authorized `P6` `E-n` that was never implemented is invisible to every existing check: `-Reconcile` flags only *unauthorized* files, and `G-01` only reports pass/fail per named `TO-n`. Add a check that every `E-n` in `P6` maps to at least one `TO-n` in `P8`, so an edit-point approved and then silently dropped is caught the same way an unapproved one is.
