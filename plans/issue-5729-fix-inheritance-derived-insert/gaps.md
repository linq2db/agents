# Review gap ledger: issue-5729-fix-inheritance-derived-insert

Two review rounds, two ledgers. Round 1 is below; round 2 follows it, and the continuity between
them — which round-1 recommendations were adopted and which were not — is recorded at the end.

## Round 1 — reviewed HEAD `cf8b19e166`

**PR:** [#5840](https://github.com/linq2db/linq2db/pull/5840) · **Reviewed HEAD:** `cf8b19e1669fcc7c8d558af578ed459f2f4399c7` · **Findings:** 5 (0 BLK, 0 MAJ, 1 MIN, 1 SUG, 3 NIT)

## Attribution

- **MIN001** — GAP-02 — `P7` Search 4's own characterization sentence: *"`TableBuilder.TableContext.cs:456` then matches by `ColumnDescriptor.MemberInfo.EqualsTo(member, …)` — **member identity, not ref type**"*, plus the same row's *"`:518-524` adds each subtype `ColumnDescriptor` verbatim to `Columns`"* which `P7` Search 6 already knew to be conditional (`EntityDescriptor.cs:526-545`). The plan never opened `EqualsTo` — it is at `Source/LinqToDB/Internal/Extensions/ReflectionExtensions.cs:838-861`, in the *same file* `P1`'s scope boundary already cites at `:102-127`, and it matches on name plus `IsSameOrParentOf` in either direction, never identity. The imprecision then shipped verbatim in the `<remarks>` at `UpdateBuilder.cs:638-641`. Secondary class GAP-03 (the predicate helper the Search-4 verdict rests on was never itself searched), but GAP-02 is earlier: the plan asserted the semantics rather than asking. — preventable: **yes**
- **SUG001** — GAP-07 — `P8`: TO-1/TO-2/TO-3/TO-7 (and TO-8) name what to assert but never a provider scope, while TO-4 alone does (*"Follow the per-file `Feature*` provider-constant pattern at `InsertWithOutputTests.cs:20-25`, not a new attribute"*). The governing convention exists in the corpus — `.claude/agents/test-writer.md:84` (*"Provider-agnostic behavior → `[DataSources]`"*) and `.claude/docs/test-review-checklist.md:48` — but `/work-plan` step 4 embeds only `.claude/rules/` path-scoped rules and KB area docs, so nothing put it in front of the author when `E-4` (`Tests/Linq/Linq/InheritanceTests.cs`) was written. Not GAP-05: every obligation exists and goes red; the defect is breadth, at `InheritanceTests.cs:1434` and its five siblings. — preventable: **partly**
- **NIT001** — GAP-10 — no plan block carries per-token formatting, and the only artifact that could have caught `EnsureDeclaringType(targetRef,assignment.MemberInfo)` at `UpdateBuilder.cs:668` is a formatter: `IDE0055` is `severity = none` repo-wide by deliberate decision (`.editorconfig:470-471`, *"linq2db style is not representable by .editorconfig; specifically vertical alignments"*), and the added-line whitespace shapes `agent-rules.md` does enumerate (trailing whitespace, 3+ blank lines, misaligning tabs/spaces, wrong indentation) do not include intra-argument spacing. What did not exist is any enforcement surface, not any information — `G-06` as defined (*"No unrelated reformatting / renames"*) is a different check and would not have fired on a line the PR itself adds. — preventable: **no**
- **NIT002** — GAP-10 — same as NIT001, second call site (`UpdateBuilder.cs:677`, parameters arm). One keystroke class, two instances; no upstream artifact, and manufacturing one would mean reversing the `.editorconfig:471` decision. — preventable: **no**
- **NIT003** — GAP-06 — the rule was in front of the author the whole time and was not applied: `agent-rules.md` → *Push to remote rules*, first bullet — *"After every successful push, check for a PR on the branch. If one exists, diff the new commits against its body."* `agent-rules.md` is always-loaded, so this is not a step-4 embed miss. The commit that dropped the `Issue5729_` prefixes (itself made in response to a review comment) falsified the body's "Known remaining" section, and no `G-nn` covers body-vs-code drift. The rule's wording aims at *new features* diverging from the body, which is why a *rename* commit did not trigger it. — preventable: **yes**

## Aggregate

The ledger is **flat** — five findings across four classes, with no two substantive findings sharing one — so there is no systemic planning failure here, only individual misses, and that is the result worth reporting on a Tier-M change of this shape.

Notably, `P7`'s *site census* held under review: 21 `ParseSetter` call sites enumerated, E-3 restored by measurement after a wrong critic-driven drop, `G-06` reconcile 0 unplanned, and no finding says a site was missed. The one impact-map failure was in a **characterization sentence** about a predicate helper, and its cost was not a missed edit but a shipped doc comment repeating the plan's paraphrase (MIN001).

Two of the five findings (NIT001/NIT002) are one keystroke slip with no possible upstream artifact — `IDE0055` is `none` by design — and a third (NIT003) is prose drift outside every gate's scope, so three of five findings are editorial with a combined durable-fix value of one wording example. No single change would have prevented more than one finding.

The highest-value fix is the `P7` predicate-citation rule below, because reading `EqualsTo` would also have pre-answered the `new`-shadowing observation the review surfaced separately — the comment `// Looks like it will not handle "new" properties case properly` sits at `ReflectionExtensions.cs:857`, one line inside the method the plan characterized without opening.

## Recommended durable fixes

> Not applied from inside the review — surfaced for the user to route to `/session-reflect`'s `plan-rule` bucket.

- **&lt;GAP-02 × 1&gt;** → the scout brief in `work-plan/SKILL.md` step 5 — add a required question for any `P7` row whose verdict is *"retyping is sufficient / the lookup will still resolve"*: **which predicate decides the match, and what exactly does it compare?** The row must cite the predicate's own `file:line` and quote its comparison, never paraphrase it; "matches on member identity" vs `EqualsTo`'s name-plus-`IsSameOrParentOf` is the worked example, and its in-source `new`-properties caveat is what a paraphrase costs you.
- **&lt;GAP-07 × 1&gt;** → `/work-plan` step 4's embed list, with the row shape in `work-plan.md` → *P8 Test obligations* — when `P6` carries a `Tests/` edit-point, embed `testing.md`'s provider-scope bullet and `test-writer.md:84`, and require every `TO-n` to name its provider attribute the way TO-4 already did. An obligation that names an assertion but not a scope defaults to whatever the author pasted from the previous test.
- **&lt;GAP-06 × 1&gt;** → `agent-rules.md` → *Push to remote rules*, first bullet — extend the body-reconciliation check with the rename case as a do/don't: a follow-up commit that **renames or removes an identifier the body names** (a test method, a type, an option) falsifies the body even though it adds no feature; re-read the whole body, not just append a `## Follow-up commit` section.
- **GAP-10 × 2** gets no durable fix by design.

---

## Round 2 — reviewed HEAD `5a34f1c222`

**PR:** [#5840](https://github.com/linq2db/linq2db/pull/5840) · **Reviewed HEAD:** `5a34f1c22270afb0703a235210eeb53f34d7e7c5` · **Findings:** 7 (0 BLK, 0 MAJ, 3 MIN, 1 SUG, 1 NIT, 2 OOS) · **Review commits:** `5a34f1c222..6ebffb20e5` · **Dominant class:** GAP-08 · **Unpreventable ratio:** 0.14

### Attribution

- **MIN001** — GAP-08 — `P8` TO-6 ("a member belonging to no mapped type still errors rather than being dropped — proof: **control**") shipped with no test among the six in `E-4`, and `P9`'s G-01 basis — "each obligation's red state was observed on unfixed code … via the `.build/.agents/repro5729` grid" — is untrue for TO-6: the 17-cell grid in `P7` Search 7 has no unmapped-member cell, so the gate certified a proof mode it never observed — G-01 — preventable: partly (the `<summary>`-vs-condition wording is implementation prose no P-block governs — GAP-10-shaped; what the absent control foreclosed is the silent-drop escalation that then cost a measurement round)
- **MIN002** — GAP-08 — `P8` TO-4 as re-anchored by U-2 onto `P7` row 7 (`UpdateBuilder.cs:295`, `InsertBuilder.cs:236`, `DeleteBuilder.cs:95`) and row 6 (`MergeBuilder.cs:102`) shipped with no test; the impact map listed the sites and the obligation named them, so this is a gate miss and not a scout miss — G-01 recorded `pass, partially` on `total: 13 succeeded: 13` whose only stated qualification was the SQL Server leg, and Search 7's grid has no `OUTPUT … INTO` cell either — G-01 — preventable: yes
- **MIN003** — GAP-06 — `P8` TO-4's verbatim instruction "Follow the per-file `Feature*` provider-constant pattern at `InsertWithOutputTests.cs:20-25`, not a new attribute" (that file does carry six such constants at `:20-25`, incl. `FeatureInsertOutputInto = AllSqlServer`) was written and carried into none of the six tests, which all took `[IncludeDataSources(true, AllSQLite, AllSqlServer2016)]` — `TestProvName.cs:166` makes `AllSqlServer2016` exactly 2016 and `:180` is the version-floor constant; the other five tests had no embedded convention at all (`testing.md:313,316`, `test-review-checklist.md:46-48` are not in `work-plan/SKILL.md` step 4's embed list — a GAP-07 shape), but an instruction ignored inside the plan's own `P8` is evidence that embedding more rules would not have carried this one — — — preventable: partly
- **SUG001** — GAP-05 — `P8` TO-3 ("a derived initializer in each of its three lambdas in turn — they share one context ref, so one test per position") cannot separate a satisfying test from a non-satisfying one: `new Child1 { Id = 1 }` *is* a derived initializer whose only member is `BaseClass.Id`, so the key-selector position is unfailable by construction, and the obligation required no seeded row, so the update branch need never execute; "one test per position" also became one test covering three, and G-01 passes legitimately because the single test is red pre-fix at the insert setter and green after — — — preventable: yes
- **NIT001** — GAP-10 — no plan block carries it: the `<remarks>` clause was introduced by `bdf0080db` during implementation, after both critic passes, and nothing in the schema prescribes doc prose for a file-private helper — `P5` D-2 fixes the helper's *behaviour* and `P7` Search 4 its *mechanism* (`TableBuilder.TableContext.cs:456` matching by `MemberInfo.EqualsTo`), neither of which is a statement about what a remarks sentence may claim about `ReflectionExtensions.EqualsTo` — — — preventable: no
- **OOS-1** — not-a-gap: out-of-plan-scope — `P7` Search 6: "`EntityDescriptor.cs:526-545`: a derived member whose *name* collides with an already-merged member goes to `_inheritanceSiblingColumns` rather than `Columns`, and gets a `SqlField` only if its physical column name is new (`SqlTable.cs:86-98`); otherwise it resolves through the inheritance fallback at `TableBuilder.TableContext.cs:485-496`. — deferred: outside the reported shape; TO-6 pins that it still errors rather than silently dropping" — a row with a reason, filed as #5852
- **OOS-2** — GAP-02 — `P2` SC-8 and `P8` TO-8 assert as fact that the shape still fails downstream on #5838 after the fix ("the call still fails downstream on #5838, so this cannot be a plain 'it works' assertion until that lands"); `P4` carries no row for it and only the *pre-fix* red was measured (`P6`'s E-3 note: `ArgumentException` from `Expression.Property` at `ParseSet:609`), so the assertion was specified into a `catch` the fixed code never enters — the GAP-05 shape is the consequence, not the cause — G-01 — preventable: yes

### Aggregate

The class distribution is flat (GAP-08 ×2, one each of GAP-06, GAP-05, GAP-02, GAP-10, plus one not-a-gap), but that spread is an artifact of attributing to the earliest artifact: four of the seven — MIN001, MIN002, SUG001, OOS-2 — sit on **one** surface, a `P8` obligation that never produced a test capable of showing what it certifies, absent (TO-4, TO-6), exercising a different branch (TO-3), or with an assertion that never executes (TO-8). Nothing in `P9` could see any of it, and that is the answer to the first question: **no gate as currently defined catches this, because G-01's unit of evidence is the run and not the obligation** — `total: 13 succeeded: 13` counts tests, so an obligation with no test, a test that skips its branch, and a test whose `catch` never fires are all indistinguishable from a satisfied proof mode, and the gate's own qualifier named only the SQL Server leg. The single change that would have prevented the most is recording G-01 as a `TO-n` → test-method ledger with the observation that proves each declared proof mode actually ran (covers MIN001, MIN002, OOS-2 directly, and makes TO-3's one-test-for-three-positions visible). Finding 3 is the different failure the second question isolates — an instruction written verbatim in `P8` and not followed — and it has no gate at all, because *narrowing* a provider gate makes G-01 greener; only a rubric/checklist rule that reads the attribute catches it. Exactly one finding is GAP-10, so the design pass is not failing its calibration test here: the plan named the obligations, and nothing carried them into tests.

### Recommended durable fixes

> Not applied from inside the review — surfaced for the user to route to `/session-reflect`'s `plan-rule` bucket.

- **&lt;GAP-08 × 2, partial cover of GAP-02/GAP-05&gt;** → a `definition-of-done.md` gate (G-01) plus `P9`'s semantics in `work-plan.md` — record G-01 as one row per `TO-n` naming the test method that satisfies it and the observation proving its declared proof mode ran; an obligation with no named test is `fail`, never `pass, partially`, and "the shape's red state was seen in a probe grid" does not discharge an obligation whose grid has no cell for it (`P7` Search 7 had none for TO-4 or TO-6).
- **&lt;GAP-02 × 1, GAP-05 × 1&gt;** → `P8`'s semantics in `work-plan.md` — a `red→green` obligation is discharged only when the **green** run is observed executing its assertion; where the assertion is conditional (inside a `catch`, an `if`, a provider branch) the obligation must name what proves it fired, and an obligation whose expected failure comes from a *third-party still-open issue* takes a `P4` row probing the post-fix state rather than asserting it (TO-8 asserted #5838 still throws; six provider cells later showed no exception at all).
- **&lt;GAP-06 × 1&gt;** → `test-review-checklist.md` (`§ [DataSources] parameter on regression tests`, which today only requires *a* provider parameter) with a do/don't example — don't narrow a provider-agnostic write regression to an include whitelist when the same region uses `[DataSources]` or a dedicated feature attribute (`[InsertOrUpdateDataSources]`), and `TestProvName.AllSqlServer2016` is exactly 2016 while `AllSqlServer2016Plus` is the version floor.

---

## Continuity between the rounds

Two round-1 findings recur in round 2, and that recurrence is the most useful thing in this file.

- **Round 1 MIN001 (GAP-02) → round 2 NIT001 (GAP-10).** Round 1 found the plan's `P7` Search 4 paraphrase — *"matches by `MemberInfo.EqualsTo` — member identity, not ref type"* — wrong, and noted it had shipped verbatim into the `<remarks>`. The author corrected the remarks in `bdf0080db`. Round 2 found the **correction** still wrong: it replaced "identity" with a claim that *"identity alone would be too narrow to resolve at all"*, which `EqualsTo:846` refutes by accepting on name plus declaring-type equality before the relation branch is reached. Third version of one sentence; round 2 removed the clause rather than rewriting it again. Round 1's recommended fix — cite the predicate's own `file:line` and quote its comparison, never paraphrase — would have prevented both versions.
- **Round 1 SUG001 (GAP-07) → round 2 MIN003 (GAP-06).** Round 1 attributed the narrow provider gates to a `/work-plan` step-4 embed miss: the convention exists in `test-writer.md:84` and `test-review-checklist.md:48` but was never put in front of the author. It was not acted on, the gates did not change, and round 2 re-found the same defect — but attributed it differently, because TO-4 *did* carry the instruction verbatim inside `P8` and it was still not followed. That is evidence for round 2's reading over round 1's: embedding more rules would not have carried an instruction the plan already contained.

Round 1's third recommendation — extend the push-time body-reconciliation check to renames — also went unapplied, and round 2's PR comment had to correct two body claims by hand.

