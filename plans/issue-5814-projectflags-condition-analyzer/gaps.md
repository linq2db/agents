# Review gap ledger — issue-5814-projectflags-condition-analyzer

## Round 1 — reviewed HEAD c9a9e1ed

| # | Finding | Gap | Gate | Preventable |
|---|---|---|---|---|
| 1 | MAJ — CFG seeded only the graph entry block | GAP-05 | G-01 (12/12 pass; no fixture contains a `try`/`catch`) | partly |
| 2 | MIN — `Directory.Packages.props` comment points "below" at a pin above | GAP-10 | — | no |
| 3 | MIN — TO-5's `HasFlag` arm could not fail | GAP-05 | G-01 (pass recorded without the discrimination check) | partly |
| 4 | SUG — `ProjectFlags.cs` doc understates analyzer reach | GAP-01 | — | yes |
| 5 | SUG — only branch conditions analyzed | GAP-01 | — | partly |
| 6 | SUG — issue-scope narrowing undisclosed | GAP-01 | — | yes |
| 7 | SUG — fixture predicates all expression-bodied | GAP-05 | G-01 (pass; statement-bodied path unexercised) | yes |
| 8 | SUG — `Explain`'s first branch unasserted / unreached | GAP-05 | G-01 (pass; no test inspects message content) | partly |
| 9 | NIT — `ClimbNegations` does not cross `&&`/`||` | GAP-05 | G-01 (P12 nominated TO-3 + census; neither can see message wording) | partly |
| 10 | OOS→fix — `LINQ2DB0006` cross-file reporting | GAP-05 | G-01 (fixture is single-document; structurally cannot discriminate) | yes |

### Detail

- **1 — GAP-05.** D-3 states the algorithm as "entry block all-possible, every other block empty" with no mention of exception-handler regions, and no `TO-n` exercises a `try`/`catch`. `U-4`/`U-5` scouted atom forms and nesting (local functions, lambdas) but never asked whether a CFG region with no ordinary predecessor edge needs its own seed. Unreachable in the current tree, so `TO-6`'s census could not have found it either — a fixture was the only thing that could.
- **2 — GAP-10.** E-9 requires only "extend the comment … Comment only". Copy-editing granularity beneath any `P`-block; a misplaced spatial reference is the prose-defect shape no design document prevents.
- **3 — GAP-05.** TO-5 was rewritten in response to critic objection #4 to defeat two non-discriminating controls, but the *third* arm added at the same time was never itself checked for discrimination the way arms 1–2 were. `P9` records "pass" without the per-arm check `work-plan.md`'s own G-01 rule demands.
- **4 — GAP-01.** E-13 requires only the purpose/modifier split and the `Keys` restriction. D-4's failure-mode requires the two out-of-domain literals and the pragma to be named "or the error is unactionable" — but that was written against the analyzer's own `<remarks>` (satisfied) and never extended to E-13's target file.
- **5 — GAP-01.** Every `SC-n` is phrased around `if`-conditional shapes and D-3 is scoped to blocks with a `ConditionKind`; nothing states that narrowing as deliberate, yet D-6's descriptor wording promises broader coverage than the design delivered.
- **6 — GAP-01.** `U-4` correctly and measurably decides not to implement bitmask recognition, but neither `P2` nor `P3` obligates disclosing the narrowing when the PR carries `Fixes #5814`. The measurement existed at plan time; only the disclosure requirement was never written.
- **7 — GAP-05.** D-5's `Model` stub writes all predicates expression-bodied, while `U-4`'s own scout output shows every real predicate is statement-bodied. No `TO-n` obligates the stub to mirror production's body shape.
- **8 — GAP-05.** The two-reason `Explain()` split is an implementation elaboration of E-1 with no corresponding `P8` obligation; no `TO-n` asserts the `{1}` argument at all.
- **9 — GAP-05.** `P12`'s residual-risk note names exactly this class and nominates TO-3 and the census as the safety net — but TO-3 tests an unrelated shape and the census only diffs ids and counts, never message wording. Both nominated obligations were incapable of catching it.
- **10 — GAP-05.** TO-4c is the only obligation exercising predicate drift, but its stub puts both types in one synthetic source string compiled as a single document, unlike the real two-file split established by `P7`'s own producer sweep.

### Aggregate

Six of ten trace to **GAP-05** — and not in the blunt "no test existed" sense. The tests that *did* exist could not have gone red for the defect they nominally covered: a fixture's synthetic model diverged from production's real shape (expression- vs statement-bodied predicates; single- vs two-file layout), or a control arm added late was not re-subjected to the discipline the plan applied everywhere else, or the obligation nominated as the safety net tested a different shape entirely.

That is the more damning result given how carefully `P8` reasoned about non-discriminating controls elsewhere (TO-3's control, TO-5's two *rejected* controls). The discipline existed and was applied unevenly — missing exactly the places it was not re-run after a late addition, or was never extended to.

Three more (4, 5, 6) are **GAP-01**: requirements the design's own artifacts implied — a diagnostic's message text, an issue's expected-behaviour list, a `<remarks>` disclosure pattern already used once — that `P2`/`P3` never turned into a stated obligation. One (2) is a genuine **GAP-10**.

The highest-yield fix is not "write more tests" but **make every fixture prove it can fail**: mutate the guard it is meant to catch and confirm red, and check that a stub mirrors production's actual syntactic and file shape, before recording the obligation as satisfied. That one habit would have caught 1, 3, 7, 9 and 10.

### Recommended durable fixes

- **GAP-05 ×6 → `work-plan.md` → `P8`.** A fixture or stub standing in for a real production artifact must either mirror its actual syntactic shape (statement- vs expression-bodied, file boundaries) or state why the difference does not matter; and every control obligation must record the mutation applied to confirm it can go red, not merely that it currently passes. Extends the existing TO-3 / TO-5 discriminating-control discipline into a standing check applied to *every* control, including ones added late in response to a critic objection.
- **GAP-01 ×3 → `work-plan.md` → `P2`/`P3` derivation.** When a plan implements an issue, require `P2` to reconcile against the issue's full stated scope line by line and record any narrowing as a `P3` anti-goal carrying a disclosure obligation wherever the PR claims to close that issue; and require a diagnostic's own message text to be checked against the `SC-n` set it is meant to satisfy, so the message cannot promise more than the design delivers.
- **GAP-10 ×1 → no durable fix.** Comment-placement prose is correctly below planning granularity.

### Dispositions

All ten adjudicated in an interactive walk. Nine fixed on the branch across eight commits (`b93ec5e94`…`f3d919d5e`); finding 6 recorded in the PR body. Fixtures 12 → 17, all green; `Source/LinqToDB` Release census 0 warnings / 0 errors. No review posted — nothing was accepted for posting.

## Round 2 — reviewed HEAD ff74d1b8

| # | Finding | Gap | Gate | Preventable |
|---|---|---|---|---|
| MIN001 | MIN — `Explain`'s `uniformOverWholeDomain` branch unreachable; flagship shape gets the path-shadowing reason | GAP-05 | G-01 (TO-1 reason pin added by `5d98f183c` copied the observed output) | yes |
| MIN002 | MIN — `ReadDomain` accepts seed / return / any `\|=` target blind; three drift shapes bypass `LINQ2DB0006` | GAP-01 | G-01 (TO-4 has no arm for them) | yes |
| MIN003 | MIN — `ProjectFlags.cs:17-18` claims a modifier move is a build error; reader re-derives silently | GAP-10 | — | no |
| MIN004 | MIN — `P9` describes `22920a6c8`, not the shipped HEAD: 12 vs 18 fixtures, unreconciled `20+2` split, no `/profile-analyzers` result | GAP-08 | G-01 (stale), profile gate (never run) | yes |
| MIN005 | MIN — `LINQ2DB0006` message / class doc say `0004`/`0005` are disabled; `IsUsable` gates on `Domain` only | GAP-01 | — | yes |
| MIN006 | MIN — catch-fixture comment states the pre-fix failure in the indicative | GAP-10 | — | no |
| MIN007 | MIN — comment claims the two `NeverTrue` sites pin the reason distinction; both pass `ExcludedEarlier` | GAP-05 | G-01 (same obligation as MIN001) | yes |
| SUG001 | SUG — `P12`'s residual risk `!(a && b)` still has no fixture | GAP-05 | G-01 (no row exists) | yes |
| SUG002 | SUG — stub declares one free modifier; `Subsets` unobservable below two | GAP-05 | G-01 (stub cardinality never obligated) | partly |

### Detail

- **MIN001 — GAP-05.** `P8` TO-1 never named which reason the #5727 shape must carry, though it is derivable on paper: `P1` frames #5727 as an *impossible flag pair* and D-6 gives `0004` the "modelling mistake" audience. The obligation added by `5d98f183c` pinned `ExcludedEarlier` on `KeysWithExpandIsNeverTrue` (`ProjectFlagsAnalyzerTests.cs:141`) — an expectation read off a live run, not from the design. The commit body records the mechanism ("a `&&` gives its right operand its own branch value, so the report lands on the individual atom, which is satisfiable somewhere in the domain"), records the consequence ("`Explain`'s other branch needs a compound evaluated in one branch; no fixture … has that shape"), and dispositions it "left alone". The defect was observed, written down, and pinned as expected behaviour. The report site is `ProjectFlagsAnalyzer.cs:444` (`model.Explain(inner, …)` on the block's single-atom node); the branch is `:1046-1048`.
- **MIN002 — GAP-01.** SC-4 (`P2:26`) promises `LINQ2DB0006` for a change the reader "**cannot parse**"; D-1's failure mode says "a shape the reader does not parse breaks the build". Neither names the closed-grammar requirement that makes a fail-safe testable — *every statement the reader accepts is fully interpreted* — and D-1's only grammar directive ("match structurally, never positionally") pushes toward blind acceptance with no counter-rule. The section-level grammar *is* closed (`:702-713` fails on anything unrecognised) and TO-4a exercises exactly that; the method-level grammar at `:665-667` and `TryReadFlagAdd`'s untested `Left` had no `SC`, no `D-n` sentence, and consequently no TO-4 arm (downstream GAP-05).
- **MIN003 — GAP-10, prose floor.** The sentence is in the original commit `22920a6c8`. E-13 named the content — the split and the `Keys` restriction, which `:6-13` states correctly — and `:17-18` goes beyond it to describe *enforcement*, contradicting D-1 ("*linked* to the code it describes") and the plan's own probe P-2 (`add Keys to the Expand arm ⇒ P-1's diagnostic stops firing`). Nothing was unknowable; no P-block carries sentence-level correctness. The nearest upstream artifact is P-2, which `P9:266` lists with **no recorded result** — a recorded result would not have made the sentence impossible, only put the contradiction on the same page as E-13. Round 1's #4 fix (`f3d919d5e`) appended `:20-26` directly beneath the false sentence without touching it.
- **MIN004 — GAP-08.** Three defects in one record. (a) `P9:241` says `total: 12`; the file declares 18 `[Test]`s. The six added by `b93ec5e94` / `b394d942d` / `ff74d1b82` have no `P9` row, although re-runs at 17 and 18 are recorded — in round 1's `Dispositions` line and in `ff74d1b82`'s commit body, never in the plan. (b) TO-6's row (`P9:252`) records a run total (`20 × 0004 + 2 × 0005`) where `work-plan.md` → `P8` ("an obligation that names a *set* … one `G-01` row per element … `78/78` cannot tell a covered element from an absent one") required one row per `P7` site; that is a GAP-06 sub-shape and the reason the split's failure to reconcile with 7 + 4 sites went unnoticed. (c) `/profile-analyzers` is listed at `P9:261` with no result at all, where `P9`'s own rule says a `skipped` must name what is unverified. The `P9` block that ships describes the tree at approval, not at review.
- **MIN005 — GAP-01.** SC-4 (`P2:26`) still reads "produces `LINQ2DB0006` **and** silences `LINQ2DB0004`/`0005` for that compilation". TO-4 was corrected 2026-09-05 (`P8:231`) to the opposite for arm (c) — inline in `P8`, with no `P11` entry — and SC-4, D-2's failure-mode sentence ("disables the condition rules"), the descriptor at `ProjectFlagsAnalyzer.cs:69` and the class doc at `:22` were all left at the approved wording. `IsUsable => !Domain.IsDefaultOrEmpty` (`:562`) is the corrected behaviour; the message is faithful to the *uncorrected* plan. Round 1's durable fix #2 ("check a diagnostic's message against the `SC-n` set") could not have caught this: the `SC` it would check against carries the same error.
- **MIN006 — GAP-10, prose floor.** The comment was written by `b93ec5e94` (round 1 #1's fix), whose body has the same clause in the past tense as a description of the pre-fix failure ("an impossible pair written inside a catch was not reported at all"); the fixture comment carries it in the indicative. A mood error; nothing unknowable.
- **MIN007 — GAP-05.** The prose face of MIN001's obligation defect. `:122-124` asserts the two `NeverTrue` sites tell a reader "whether the clause is a modelling mistake or dead code"; both sites pass `ExcludedEarlier` (`:141`, `:207`), so the pair cannot go red for the distinction it claims to pin. `work-plan.md` → `P8` line 118 — the rule round 1 wrote — asks for the *mutation* that proved a control can go red; `5d98f183c` recorded an *observation* instead, and the comment describes the intended affordance rather than what the fixture holds.
- **SUG001 — GAP-05.** `P12` (`plan:311`) names `!(a && b)` as the single residual risk and nominates TO-3 and the census; round 1 #9 found both incapable. `364c5cd5f` addressed #9 by changing the `0004` message's trailing sentence (`ProjectFlagsAnalyzer.cs:51`, one line) — `ClimbNegations` (`:482`) is untouched, `P8` is untouched, and no fixture negates a compound. The obligation `P12` called for was never written into `P8`, before or after round 1.
- **SUG002 — GAP-05.** D-5 specifies the stub only as "declare their own stub `ProjectFlags` / `ProjectFlagExtensions` / `GetProjectFlags`". D-7 reasons explicitly about the free-modifier powerset ("a third free modifier takes the domain to 104") yet no obligation requires the stub to carry the two free modifiers at which `Subsets` (`:757-759`) first does anything; the stub's `GetProjectFlags` (`ProjectFlagsAnalyzerTests.cs:98-99`) has one. `work-plan.md` → `P8` line 120 (round 1's stub-mirror rule) names two axes — syntactic form and file layout — and `d85f48687` satisfied it literally on the first. **Partly**: the class was named; this axis was not in the rule's wording, and the census (real model, two free modifiers, 0 false positives / 11 true) does exercise `Subsets` at two bits end-to-end, so the exposure is fixture-level.
- **Out-of-scope observation (no CI leg runs `0004`–`0006` against `Source/LinqToDB`) — not attributed as a finding.** If it were: GAP-02. `P3:63` and TO-6 assert "the analyzer runs on every Release build of `Source/LinqToDB`" citing `Directory.Build.props:120-122`; `P7`'s CI sweep searched `Tests\.Analyzers` for the *test project's* registration and never `RunAnalyzersDuringBuild` / `with_analyzers` in the build templates — a grep that would have settled it. It is also what makes MIN004 load-bearing: with no CI leg, `P9` is SC-6's only evidence.

### Aggregate

GAP-05 ×4 (MIN001/MIN007 are one defect), GAP-01 ×2, GAP-08 ×1, GAP-10 ×2. GAP-05 still dominates, but its shape has moved: round 1's were fixtures that *could not go red*; round 2's are a fixture that goes red on any change yet pins the **wrong expectation** (MIN001/MIN007), an obligation the plan's own critic named that was **never written** (SUG001), and a mirror axis round 1's rule **did not name** (SUG002). The second theme is new: **the plan stopped being maintained at approval.** `P9` describes `22920a6c8` (MIN004), SC-4 describes the pre-correction contract (MIN005), and `P11` has no entry for the TO-4 correction, the D-3 widening in `b394d942d`, or the eight round-1 fixes — so round 2 reviewed a branch whose plan was true of a different tree. The single change that would have prevented the most — five of nine (MIN001, MIN007, SUG001, MIN004, MIN005) — is **writing every post-approval change back into the plan before the fix commit lands**: the `TO-n` with its expected value derived from `P1`/`D-n` rather than from the run, the `SC-n` it alters, and the `P9` row it invalidates.

Round 1's headline — *make every fixture prove it can fail* — is supported in class and refined in content. It is necessary and not sufficient: `5d98f183c` produces a fixture that provably fails on any change to `Explain`, and it is still wrong, because its expected value was read from the output it was meant to judge. `P8`'s own `red→green` definition already says "for the right reason"; the refinement is that the reason comes from `P1`/`D-n` on paper, and a pin whose expectation was first seen in a run is an observation, not an obligation.

### Recommended durable fixes

- **GAP-05 ×4 → `work-plan.md` → `P8`, two amendments to the rules round 1 added at lines 118 and 120.** (i) A `TO-n` that pins a message or reason argument derives the expected value from the `P1`/`D-n` block it cites *before* the first run; an expectation first observed in output is recorded as an open question, not an obligation. A residual risk `P12` names becomes a `P8` row before the verdict is recorded, and a fix commit for a finding of the form "no obligation can see X" carries the obligation, not only the code. (ii) The stub-mirror rule gains a third axis, **model cardinality**: the stub carries at least the smallest count at which every combinatorial construct in the code under test is exercised (here ≥ 2 free modifiers for `Subsets`), and states the count it chose.
- **GAP-08 ×1 + GAP-01 ×1 (MIN005) → `work-plan.md` → `P9` / `P11` semantics, plus `work-plan.ps1 -Action reconcile`.** Every `P9` row names the commit it describes and is re-derived after the last commit touching any `E-n`; a listed probe or extra gate with no result is recorded `fail`, never left blank. A mid-implementation `TO-n` correction that changes a *contract* (not just a fixture) is a `P11` amendment that lists every block restating that contract — `SC-n`, the `D-n` failure mode, descriptor text. Mechanisable half: `reconcile` flags a `P9` `G-01` fixture count ≠ the `[Test]` count in the `E-n` test files, and a `P9` older than the newest commit touching an `E-n`.
- **GAP-01 ×1 (MIN002) → `plan-critic.md` attack vector + `work-plan.md` → `P2` derivation.** An `SC-n` promising a fail-safe ("anything the reader cannot parse fails loudly") must name the *closed accepted set*; the critic attacks it by listing the inputs the reader would accept without interpreting — for a syntactic reader, every statement kind the enumerated grammar does not mention (seed, return, assignment target).
- **GAP-10 ×2 → no durable fix.** Sentence-level prose; correctly below planning granularity.

### Continuity

Round 1's two durable recommendations *were* written into the corpus (`work-plan.md` → `P8` lines 118 and 120, both citing #5877); round 2 measures the limits of their wording, not their absence.

- **#8 → MIN001 + MIN007 — applied-but-aimed-wrong.** The recommendation ("assert the `{1}` argument") was executed in `5d98f183c`, and the execution *produced* the evidence of the deeper defect — the commit body states that the flagship shape emits the dead-code wording and that the model branch "needs a compound evaluated in one branch" no fixture or production site has. Both facts were dispositioned "left alone" and the observed wording was pinned as the expectation. The comment MIN007 targets was written in that same commit, claiming the affordance the body says is not exercised.
- **#9 → SUG001 — unapplied.** Round 1's row said the nominated obligations "were incapable of catching it". The fix (`364c5cd5f`) is a one-line message-wording edit at `ProjectFlagsAnalyzer.cs:51`; `ClimbNegations` and `P8` are unchanged. The one artifact round 1 identified as able to see the shape — a fixture — was not added.
- **#7 → SUG002 — applied, aimed narrow.** `d85f48687` mirrored production on the axis the finding named (body shape). The rule as written names syntactic form and file layout; model cardinality is a third axis it does not reach, and the stub's comment ("a faithful miniature of the real model") still stands over a model with one free modifier against production's two.
- **#4 → MIN003 — a round-1 miss in the same block, not a recurrence.** `f3d919d5e` extended the `ProjectFlags.cs` doc with `:20-26` and left the already-false `:17-18` directly above it untouched. Neither unapplied nor aimed wrong: the fix landed beside an error the round did not see.
- **#1 → MIN006 — defect introduced by the fix.** `b93ec5e94` wrote the comment. The same fix also needed a second fix (`ff74d1b82`, Copilot-reported over-seeding), whose refuted claim — "an all-possible state can only ever produce fewer reports" — is the same pattern as MIN006/MIN007: a fix's own comment asserting a property nobody measured. Not in round 2's set; noted for the pattern only.
- **Round 1 `Dispositions` → MIN004(a).** "Fixtures 12 → 17, all green; census 0 warnings / 0 errors" is the re-verification `P9` should carry. It lives in this ledger and in `ff74d1b82`'s commit body (17 → 18), and `P9` still says 12.

## Round 3 — reviewed HEAD a1516f71b

| # | Finding | Gap | Gate | Preventable |
|---|---|---|---|---|
| MIN001 | MIN — `ReadDomain` skips a zero-add switch section on a comment's word, never testing for a `throw` | GAP-01 | G-01 (26/26 pass; zero-add guard removed → 1 fixture reddens) | partly |
| MIN002 | MIN — `Explain`'s path-shadowing reason is self-contradictory; carried by all 22 census diagnostics | GAP-05 | G-01 (26/26 pass; the wrong wording is itself `TO-1`/`TO-2`'s pinned expected value) | partly |
| MIN003 | MIN — lambda descent walks only `block.Operations`, missing `block.BranchValue` | GAP-05 | G-01 (26/26 pass; `BranchValue` descent removed → 1 fixture reddens) | partly |
| MIN004 | MIN — both `messageFormat`s prescribe an edit that is wrong for 2 of 4 operator/polarity combinations | GAP-05 | G-01 (26/26 pass; no `TO-n` covers the `&&` polarity where deleting is wrong) | partly |
| MIN005 | MIN — no fixture invokes `IsSqlOrExpression`; `Atom.Holds`'s `AnyOf` branch unexercised | GAP-05 | G-01 (26/26 pass; `Atom.Holds` forced to `AllOf` → 2 fixtures redden) | yes |
| MIN006 | MIN — TO-5 comment cites two files as real instances of the fixture's shape; neither is | GAP-10 | — | no |
| SUG001 | SUG — a braced `case` body falls to the unrecognised-statement arm, a Release build error for a cosmetic edit | GAP-05 | G-01 (26/26 pass; brace flatten removed → 1 fixture reddens) | yes |
| NIT001 | NIT — fixture-count comment stale (3→6→7→8) | GAP-10 | — | no |
| NIT002 | NIT — comment claims `Evaluate`'s `And`/`Or` coverage; no fixture used a non-short-circuit disjunction | GAP-05 | G-01 (26/26 pass; `Or` arm forced to return `null` → 1 fixture reddens) | yes |
| — | local-function descent: `graph.LocalFunctions` had no fixture; deleting the loop left the suite green | GAP-05 | G-01 (26/26 pass; `LocalFunctions` descent removed → 1 fixture reddens) | yes |
| — | `CollectTrackedSymbols`' write-drop guard, called load-bearing by its own doc, had no fixture | GAP-05 | G-01 (26/26 pass; write-drop guard removed → 1 fixture reddens) | yes |

### Detail

- **MIN001 — GAP-01.** `D-1` states "Every `ProjectFlags` member except `None` must land in one of those buckets, or the model is rejected" — a completeness rule about **enum members**, not about whether a given switch *arm*'s zero-add contribution is legitimate. Nothing in `P2`/`P5` enumerates the closed grammar of "a recognised zero-add arm" (only a `throw`; a bare `break` is unclassifiable and must fail loudly). Same missing-closed-grammar class round 2 filed as `GAP-01` for its MIN002: round 2's fix (`f0874f5ee`) hardened three blind-acceptance shapes in this very function — seed, return, `|=` target — but never asked "does a *legitimate* zero-add arm exist, and how do we tell it from garbage?", a fourth shape its own sweep wording ("every statement kind the enumerated grammar does not mention") should have reached. **Partly**: the class was named at round 2; this arm slipped past the remediation that closed the other three.
- **MIN002 — GAP-05.** Round 2's standing `P8` rule — a `TO-n` pinning a message derives its expected value from the `P1`/`D-n` block it cites, never from the first run — was applied by `f0874f5ee` to add *new* coverage, but never swept backward over the **existing** `ExcludedEarlier` constant. The string has been unchanged since `22920a6c8`; round 1's `5d98f183c` pinned it as a test literal and round 2's `f0874f5ee` built a sibling fixture beside it, both touching the mechanism without anyone re-reading the sentence as English. **Partly**: the rule that should have caught it exists and was applied in the same commit, just not to the pin that inspired it.
- **MIN003 — GAP-05.** `D-3` commits to nested-graph analysis without saying a lambda can occupy either `block.Operations` or `block.BranchValue`. The **reporting** loop twenty lines above already makes exactly that distinction, with a comment explaining why. No `TO-n` mirrors the two-position coverage for the **descent** loop, so it shipped narrower than its own sibling — in `AnalyzeGraph`, the function `b93ec5e94` and `ff74d1b82` both edited. **Partly**: the pattern to copy sat in the same file, but `D-3` never named the split.
- **MIN004 — GAP-05.** `D-3`'s outermost-sub-expression choice is restated in `P10` as "names the exact text to delete" — an implicit claim over the full `&&`/`||` × `0004`/`0005` matrix. `TO-1`/`TO-2` exercise only the two cells where deleting the clause is safe; the `&&` cell where it is wrong is live in this PR's own census (`TableBuilder.TableContext.cs:229`). **Partly**: the enumerated-set rule for this shape is already in the corpus; the matrix was implicit in `P10`'s restatement rather than written as four rows.
- **MIN005 — GAP-05.** `D-2` names `IsSqlOrExpression` explicitly as the one predicate read as a bitmask rather than a `HasFlag` — "the only one of the 14 that is not a `HasFlag` call". A decision naming a distinguished member is precisely the enumerated-set `P8` trigger, and no `TO-n` ever invoked it. **Yes**: a `TO-n` was one row away.
- **MIN006 — GAP-10.** `P8`'s own TO-5 text is correct — it explains why those two files are *rejected* controls. The test-file comment paraphrased that into a stronger, wrong claim during implementation. No `P`-block granularity governs prose inside a test comment, and the source of truth was already right.
- **SUG001 — GAP-05.** `D-1`'s failure-mode line predicts this exact hazard: *"if the reader matches statement order rather than structure, the tax arrives for cosmetic edits too."* No `TO-n` tested the mitigation against a braced restructuring — in the same loop `f0874f5ee` edited internally. **Yes**: `D-1` names the hazard in so many words.
- **NIT001 — GAP-10.** A comment naming a fixture count, stale on every addition. Same class as round 1's #2 and round 2's MIN003/MIN006.
- **NIT002 — GAP-05.** `D-3`'s CFG-lowering design implies `Evaluate` needs both a non-short-circuit `And` and `Or` arm, since `&&`/`||` are lowered into separate blocks and never reach `Evaluate`. A comment claimed both were covered; only `And` was. Same enumerated-pair shape as MIN005, one function over. **Yes.**
- **local-function descent — GAP-05.** `D-3` names `LocalFunctions` explicitly alongside `IFlowAnonymousFunctionOperation`. The descent was correct from the start and had zero fixtures. Found incidentally while writing MIN003's fixtures. **Yes**: `D-3` names the kind by name.
- **`CollectTrackedSymbols` guard — GAP-05.** `U-2`'s scout note says outright: *"The analyzer still bails on a mutated tracked symbol as a safety net; it is simply never triggered today."* A safety net whose untested status was recorded at plan time and never turned into a `TO-n`. **Yes**.

### Aggregate

Seven of eleven trace to `GAP-05`, two to `GAP-01`, two to `GAP-10` — and the `GAP-05` shape is neither of the first two rounds'. Round 1's fixtures existed but could not fail; round 2's failed but pinned a wrong expectation. Round 3's is a third mode: **the mechanism had no fixture at all**, and mutating it in isolation — forcing `Atom.Holds` to `AllOf`, deleting a descent loop, removing a guard — left the entire 26-fixture suite green. Three of the eleven (MIN001, SUG001, MIN003) sit inside functions round 1's and round 2's own fix commits edited directly, without the author noticing the sibling gap a few lines from their own change.

The highest-yield fix is not "pin the right value" (round 2's lesson) but **sweep every branch of the implementation itself for a fixture, not just every `P2` criterion**. The reviewer did this by hand here; nothing in `P8` requires an author to do it before shipping.

### Recommended durable fixes

- **GAP-05 ×7 → `work-plan.md` → `P8`, a third amendment beside round 1's and round 2's.** Extend the "control obligation records the mutation" rule from *controls* to **every conditional, loop and guard in a new analyzer/detector's own implementation** (`E-1`-class edit-points): before `G-01` is recorded `pass`, the branches of the implementation are enumerated and each shown to redden under its own targeted mutation. Would have caught MIN001, MIN003, MIN005, SUG001, NIT002 and both unlabeled findings — seven of eleven.
- **GAP-01 ×2 → `work-plan.md` → `P5` derivation.** When a `D-n` names a **distinguished set** the implementation must recognise (switch-arm shapes in a syntactic reader, `Atom` forms, nested-graph kinds), the enumerated-set rule must reach **implementation grammar**, not only business-facing shapes. MIN001's gap is a switch-arm shape the closed grammar never named — the same class as `D-2`'s named-predicate case (MIN005) but one level lower, in syntax rather than semantics.
- **GAP-10 ×2 → no durable fix.** Fixture-count and citation prose in test comments; correctly below planning granularity, and 2 of 11 is a reasonable floor.

### Continuity

Round 1's headline (*make every fixture prove it can fail*) and round 2's refinement (*the expected value comes from `P1`/`D-n`, not from a run*) are both in `work-plan.md` → `P8`, both citing #5877. Round 3 shows all three possible outcomes at once, for the first time.

- **Applied, and it worked — `P9` re-derivation.** Round 2's `P9`/`P11` rule is fully honoured on this branch: `G-01` and TO-6 carry explicit "re-measured against `ff74d1b82`" and, now, "re-measured after the round-3 fixes" lines with fresh totals. The one clean adoption, and why round 2's MIN004 staleness class did not recur. `G-02` also moved `blocked` → `pass` this round on CI evidence.
- **Applied downstream, not upstream — round 1's mutation rule.** The fix commit `9838672d8` names, for each of its seven mechanisms, the exact mutation and the fixture that reddened. Textbook execution — but the rule's wording scopes it to *control obligations already in `P8`*, and none of MIN001/MIN003/SUG001/NIT002/the two unlabeled findings had a `P8` row. The rule did not bind them at authoring time; the **reviewer** applied its spirit. It prevented nothing here, it only supplied a repeatable technique for finding what the plan never obligated.
- **Applied, but not swept backward — round 2's derive-from-design rule.** `f0874f5ee` used it correctly for its own additions and never re-applied it to the **existing** `ExcludedEarlier` constant that was the literal subject of the round-2 finding it answered. A rule that fixes future pins does nothing for one that predates it — except here the rule and the defective pin are contemporaries in the *same commit*, a narrower miss than `work-plan.md`'s stated authoring-time limit anticipates.
- **Three findings sit inside code rounds 1 and 2 themselves touched.** MIN001 and SUG001 are in `ReadDomain`'s switch-section loop, the loop `f0874f5ee` edited while threading an `accumulator` parameter through every `TryReadFlagAdd` call in it. MIN003 is in `AnalyzeGraph`, edited by both `b93ec5e94` and `ff74d1b82`, eight lines below a reporting loop whose own comment states the exact split the descent loop lacked. In all three the fix author was already inside the function that needed the sibling fix. That is a stronger claim than "the plan didn't obligate it" — the remediation work was the nearest possible vantage point and still missed it, which is why the recommended fix is a structural branch sweep rather than closer reading.

### Dispositions

Twelve items walked interactively. Nine code findings fixed on the branch in a single commit (`9838672d8`), plus two coverage gaps the reviewer surfaced mid-fix and one already-tracked CI observation; MIN007 (stale PR body) approved as a body edit. Every fix pinned by a fixture proved to redden under its own mutation and no other. Fixtures 26 → 37, all green; `Source/LinqToDB` Release census re-measured at 0 warnings / 0 errors. No review posted — nothing was accepted for posting, as in rounds 1 and 2.

## Round 4 — reviewed HEAD 9838672d

| # | Finding | Gap | Gate | Preventable |
|---|---|---|---|---|
| MIN001 | MIN — `NeverTrue.description` still claims the guarded code never runs; false for a disjunct | GAP-01 | — (no gate or fixture can read a `description`) | yes |
| MIN002 | MIN — `AlwaysTrue.description` names only the reason the census measured firing zero times | GAP-01 | — (`P9`'s TO-6 row already carries the falsifying measurement) | yes |
| MIN003 | MIN — a missing `ProjectFlagExtensions`/`ExpressionBuildVisitor` retires both rules with no `LINQ2DB0006` | GAP-05 | G-01 (37/37 pass; all seven TO-4 arms mutate a member *body*, never a type) | yes |
| MIN004 | MIN — `ReadDomain`'s `count != 1` cardinality guard unreached by all 37 fixtures | GAP-05 | G-01 (37/37 pass; TO-4b tests membership, nothing tests cardinality) | yes |
| SUG001 | SUG — round 3's brace flatten closes `{ …; break; }` and not `{ … } break;` | GAP-05 | G-01 (37/37 pass; `BracedSwitchSectionIsRead` covers one of two spellings) | yes |
| SUG002 | SUG — `ClimbNegations`' parenthesis arm unreached; deleting it inverts id and polarity | GAP-05 | G-01 (37/37 pass; the only parenthesised negations are `!(a && b)`) | yes |
| SUG003 | SUG — every fixture tracks a **parameter**; `ReadSymbol`'s local arm is deletable green | GAP-05 | G-01 (37/37 pass; 27 consumer bodies, all `M(ProjectFlags flags)`) | yes |
| NIT001 | NIT — `catch (NotSupportedException)` comment misdescribes what `GetControlFlowGraph` declines | GAP-02 | — (`U-7` probed API presence, never contract) | partly |
| NIT002 | NIT — `new bool[1]` gate flag allocates per operation block | GAP-01 | `/profile-analyzers` (extra gate, pass — measures time, not allocation) | partly |
| NIT003 | NIT — fixture comment argues from production's cardinality about a stub-backed fixture | GAP-10 | — | no |
| NIT004 | NIT — TO-4f comment states a widening consequence its own mutation cannot produce | GAP-10 | — | no |

### Detail

- **MIN001 — GAP-01.** `P2` carries no criterion about diagnostic prose. The nearest artifact is **D-6** (`plan:125`), which assigns `0004` the audience *"your guarded code never runs"* — verbatim the claim `NeverTrue.description` (`ProjectFlagsAnalyzer.cs:57`) still makes — while **D-3** (`plan:99`) simultaneously commits to reporting the *outermost constant sub-expression* inside `&&`/`||` chains, where the body still runs through the other disjuncts. Two approved decisions in conflict, with nothing reconciling them. The disjunct shape is not hypothetical to this plan: `P11` A-1's own rows E-17/E-18/E-20 are three disjunct removals. Provenance is exact — the text is original `22920a6c8` (`git log -S "guards code that never runs"`, one commit) and `9838672d8` touched exactly two lines in that block, both `messageFormat`. **Preventable: yes** — round 1's `364c5cd5f` derived this very mechanism in its commit body and elected to leave the claim standing in the description, on the recorded reasoning that the description *"is not tied to a syntactic position"*.
- **MIN002 — GAP-01.** Same absent criterion, second field. `AlwaysTrue.description` (`:66`) gives only the whole-domain reason, and the plan itself records that reason firing **zero** times: `P9`'s TO-6 row (`plan:255`) — *"Every one of the 22 diagnostics carries `Explain`'s path-shadowing reason"* — written during round 2's remediation. No rule requires descriptor prose to be re-read against a gate result, and the asymmetry needs no measurement anyway: `NeverTrue.description` one line group above carries both reasons. Original `22920a6c8`, untouched since. **Preventable: yes.**
- **MIN003 — GAP-05.** The requirement *is* named: **D-2**'s failure mode (`plan:95`) says an alias or a rename *"would not resolve. Contained by D-1's fail-safe: an unparseable model reports `LINQ2DB0006`"*, and SC-4 (`plan:26`) promises the same. The obligation is what is missing. TO-4 grew from three arms to seven (TO-4a…TO-4g), and every one mutates the *body* of a model member — a switch shape, an enum member, a predicate, the seed, the return, the `|=` target, an empty arm — so all seven enter through `:96-100` with both types resolved. One more arm of exactly the shape the other six take. **Preventable: yes.**
- **MIN004 — GAP-05.** **D-1** (`plan:82`) states the model in the singular — *"the unconditional `flags |= ProjectFlags.X` in each switch section is a **purpose**"* — and the `count != 1` guard at `:789-793` is that sentence's enforcement. TO-4b covers the *membership* half (an unclassifiable member); nothing covers the *cardinality* half, so the guard is unreached by all 37 fixtures. Original `22920a6c8` code, unentered across four rounds. Round 3's class, in the file round 3 swept. **Preventable: yes.**
- **SUG001 — GAP-05.** Round 3's fix `9838672d8` added the flatten (`:735-736`) and `BracedSwitchSectionIsRead` (`ProjectFlagsAnalyzerTests.cs:654-657`), whose mutation produces `case X: { …; break; }`. C# also permits `case X: { … } break;`, which still falls to the unrecognised-statement arm at `:760` — a Release build error for a cosmetic edit, the hazard **D-1's failure mode** (`plan:87`) names in so many words. `work-plan.md:110`'s one-fixture-per-element rule covers this class but binds `P8` rows, and this fixture was written by a fix commit with no `P8` row — the gap round 2's own corollary (`work-plan.md:128`, *"a fix commit … carries the obligation"*) half-anticipates. **Preventable: yes.**
- **SUG002 — GAP-05.** `ClimbNegations`' `ParenthesizedExpressionSyntax` arm (`:496-500`, original `22920a6c8`) is unreached: the suite's only parenthesised negations are `!(a && b)` (`:253-273`), where the atom's parent is the `&&`. The arm is load-bearing for an `SC-n` — removing it reports `LINQ2DB0004` on the operand where **SC-2** (`plan:24`) requires `LINQ2DB0005` on the negation, the inversion **D-3** chose "outermost" over "smallest" to prevent. The `!(a && b)` fixtures arrived in round 2 as `P12`'s residual risk, which is why the parenthesis shape *reads* as covered. **Preventable: yes.**
- **SUG003 — GAP-05, with the stub-mirror rule one axis short again.** `U-1` (`plan:67`) records production's own idiom: the sole producer site is `ExpressionBuildVisitor.cs:460`, `var flags = GetProjectFlags();` — a **local**. All 27 consumer fixtures declare `public static int M(ProjectFlags flags)`, so `ReadSymbol`'s `ILocalReferenceOperation` arm (`:527`) deletes green, while `CollectTrackedSymbols`' own doc (`:190-194`) claims both kinds. `work-plan.md:130`'s three axes (syntactic form, file layout, model cardinality) all govern the *stub*; the **subject code** the fixture feeds the analyzer is a fourth, and the scout row that would have set it was already in the plan. **Preventable: yes.**
- **NIT001 — GAP-02.** `U-7` (`plan:73`) probed `Microsoft.CodeAnalysis.xml` for the *presence* of `GetControlFlowGraph`, `ControlFlowGraph.Create` and the nested-graph accessors, and never for their *contract* — which body shapes are declined and how. The `catch` comment (`:178-182`, original `22920a6c8`) names field initializers, and `ControlFlowGraph.Create` has an `IFieldInitializerOperation` overload in the same file the probe already had open. Nothing was unknowable; the probe asked a narrower question than the code later assumed. **Partly** — the comment is wrong, the defensive `catch` is not, and no `U-n` phrasing reliably anticipates a catch written afterwards.
- **NIT002 — GAP-01.** **D-7**'s failure-mode note (`plan:133`) names the exposure — *"an allocation per block per method, on an every-keystroke path"* — and nominates `/profile-analyzers` as *"a gate rather than a formality"*. The gate ran and passed (`P9:266`: 4.469 s, 0.28 %, rank 50/566) and measures **time**, so nothing in the plan can see a redundant `new bool[1]` per operation block (`:135`, original `22920a6c8`). No `SC-n` or `P3` line states an allocation budget for `E-1`. **Partly** — hazard class named, instrument chosen for it cannot register this.
- **NIT003 — GAP-10, prose floor.** The comment (`ProjectFlagsAnalyzerTests.cs:313-318`) was written by round 3's own fix `9838672d8` for the `Atom.Holds` `AnyOf` fixture and argues in production's numbers ("two of the ten purposes", "false for all 52") about a fixture that runs against the 6-purpose / 36-value stub. The stub *does* state its reductions (`:103-105`), so `work-plan.md:130` is satisfied; no P-block reaches a per-fixture comment's arithmetic.
- **NIT004 — GAP-10, prose floor.** The comment (`:628-630`) came from round 2's fix `f0874f5ee` (`git log -S "it would *widen* the domain"`). The fixture still discriminates — on the missing `LINQ2DB0006` — so the defect is confined to the explanation. This is the fourth instance of the shape round 2's Continuity named (*"a fix's own comment asserting a property nobody measured"*: round 2 MIN006, round 3 MIN006, round 4 NIT003 + NIT004). Recurrence noted, not reclassified — manufacturing a plan block for a fixture comment's mechanism claim would cost more than the class does.
- **Out-of-scope observation 1 (opaque boolean expression) — not attributed as a finding.** If it were: **GAP-01**, continuing round 1's #5 (*only branch conditions analyzed*) and #6 (*narrowing undisclosed*), both GAP-01. `ReportConstants`' descent set (`:467-481`: `!`, `&&`, `||`, `&`, `|`) is nowhere written down as closed; `P3` discloses the bitmask narrowing (`U-4`) and not this one, which is narrower — it drops atoms the recogniser handles perfectly.
- **Out-of-scope observation 2 (two remaining blind-acceptance shapes) — not attributed as a finding.** If it were: **GAP-01**, third recurrence of round 2's MIN002 class. The durable artifact for it *was* written — `plan-critic.md`'s closed-accepted-set attack vector, `ad94484`, 2026-09-06 16:58 — but it binds the **critic at plan time**, and this plan's `P12` pass ran 2026-09-05, so it could not have applied here. Nothing binds the **remediation** side, and remediation has closed only the shapes named to it: three in round 2 (`f0874f5ee` — seed, return, `|=` target), one in round 3 (`9838672d8` — zero-add arm), two still open.

### Aggregate

GAP-05 ×5, GAP-01 ×3, GAP-02 ×1, GAP-10 ×2 — GAP-05 dominant for the fourth round running, and in round 3's exact mode: an implementation branch with no fixture, inside the file round 3 swept. The sharper number is provenance: **eight of the eleven are in code from the plan's first implementation commit `22920a6c8`** (`git log -S` on each) that three review rounds and 37 fixtures never reached; only SUG001 (round-3's new brace flatten) and the two comment findings are newer. The design is not what is failing — `P2`, `P5` and `P7` have held up across four rounds — the *coverage record* is: `P9:252` reads "Ten fixtures were added, each closing a branch of the analyzer that no fixture could see", a sentence that is true of the seven mechanisms the reviewer handed over and cannot be distinguished from an exhaustive sweep.

The single change that would have prevented the most is to make the branch enumeration **mechanical rather than narrative**: run the fixture project under branch coverage and record the uncovered-branch list in `P9`. That flags MIN003, MIN004, SUG002 and SUG003 (4 of 11) with no judgement involved, and would have flagged five of round 3's seven. The second-largest is the diagnostic-text class (MIN001, MIN002 here; six findings across all four rounds, all inside the same ~25 lines).

The ledger is **not** GAP-10-dominant (2 of 11), so the design pass is still earning its cost. But the yield has migrated from design defects to unexercised implementation and to prose written by prior rounds' own fixes — the first is a tooling problem, the second is the floor.

### Recommended durable fixes

- **GAP-05 ×4 → `work-plan.md` → `P9`, beside the `P8` branch rule at line 124** — for an `E-n` that adds a detector / analyzer / parser, the branch sweep is recorded as an **enumeration with a denominator**, not a narrative: run the fixture project under branch coverage and put "N branches, M uncovered: `<list>`" in the `G-01` row, each uncovered entry either fixtured or waived with a reason. Add the corollary that a fixture written by a **fix commit** inherits `P8`'s one-fixture-per-element rule — SUG001's brace class has two spellings and got one.
- **GAP-01 ×2 → `.claude/docs/authoring-analyzers.md`, plus one line in `work-plan.md` → `P5`** — a diagnostic's user-facing text is **one artifact**: `title`, `messageFormat`, `description` and every reason string the message interpolates. An edit to any field re-derives the rest against the report-shape matrix the design chose (operator × polarity × reason). Do/don't straight from this PR: `NeverTrue.description` still says a never-true test *"guards code that never runs"* two rounds after that claim left `messageFormat` as false for a disjunct, and `AlwaysTrue.description` names only the reason `P9` measured firing zero times. Highest cumulative yield in the ledger.
- **GAP-05 ×1 + both out-of-scope observations → `code-reviewer.md` rubric / `review-orchestration.md` remediation rule** — when a finding is *"the reader accepts X blindly"* or *"the reader bails on X silently"*, the fix commit closes the **class**, not the instance: enumerate every input accepted without interpretation and every precondition bailed on without reporting, then close or document each. The count is the argument — round 2 closed three shapes, round 3 one, round 4 opens two more plus MIN003's silent type-resolution bail; `plan-critic.md`'s vector for this binds the plan-time critic only.

### Continuity

Round 3 made three recommendations. All three are measurable on this HEAD.

- **#1 — "sweep every conditional, loop and guard in a new analyzer/detector's own implementation … before recording `G-01` pass" → applied, aimed narrow, and the record over-claims.** It was written into the corpus verbatim at `work-plan.md:124` by `bb6931a` (2026-09-06 23:00:54) — **43 minutes after** the fix commit `9838672d8` (22:17:09) that it describes — so it never bound this branch's authoring. Execution against the reviewer's list was textbook: the commit body names the mutation and the reddening fixture for each of the seven mechanisms, and `P9:252` records all seven (`Atom.Holds`→`AllOf` 2 red, `BranchValue` descent 1, zero-add guard 1, brace flatten 1, `Evaluate.Or` 1, `LocalFunctions` 1, write-drop 1). Three unreached branches were left behind in the same two files — `count != 1` (`:789`), `ClimbNegations`' paren arm (`:496`), `ReadSymbol`'s local arm (`:527`) — every one original `22920a6c8` code and every one inside the rule's stated scope ("each `E-n` file"). **So the recommendation is not shown insufficient as worded — it is shown unfalsifiable as recorded.** Nothing in `P9` distinguishes "swept the file" from "swept the seven I was handed", and the sentence that ships reads as the former. That is the change worth making: require the denominator. A second, smaller limit shows in SUG001 — executing the technique per item is not self-checking about the item's own class, so the fix for a two-spelling hazard shipped one spelling.
- **#2 — "the enumerated-set rule must reach implementation grammar (`P5` derivation)" → unapplied.** `bb6931a`'s `work-plan.md` diff is a single hunk adding the `P8` branch rule; no `P5` change was written. Round 4's MIN003 (a resolution precondition that bails silently where D-2 promises containment) and both out-of-scope blind-acceptance shapes are that class, third round running.
- **#3 — "GAP-10 ×2 → no durable fix" → held, and the ratio reproduces exactly (2 of 11).** Both round-4 instances are comments written by *previous rounds' fix commits* — NIT004 by `f0874f5ee`, NIT003 by `9838672d8` — the pattern round 2's Continuity named and left unhomed, now at four instances. Still correctly below planning granularity; worth recording that it is the most *predictable* class in the ledger, which is the argument for reading it as a floor rather than as a lapse.
- **Second thread — MIN001/MIN002 are round 3's own fix, incomplete, and the chain runs back to round 1.** Mechanically confirmed: `9838672d8` changes exactly two lines in the descriptor block, both `messageFormat`; both `description`s are unchanged since `22920a6c8`. It is stronger than "the fix landed beside an error in the same block". Round 1's `364c5cd5f` removed the same claim from `messageFormat` and *elected to leave it standing in the description*, recording the reasoning in its body — *"leave the inference to the description, which already carries it and is not tied to a syntactic position"* — and its worked example (`!(IsSql() || IsExpression())` at `TableLikeQueryContext.cs:260`) is the identical disjunct mechanism MIN001 now names. The premise of that decision is exactly what rounds 3 and 4 both refuted. Counting the class "a fix that closes one form of a two-form class, or relocates a false claim instead of deleting it": round 1 → round 2's MIN003; round 2 → MIN006/MIN007 and NIT004; round 3 → SUG001 and NIT003; round 3 → MIN001/MIN002. It has fired in every round, and it is the only thread in this ledger that has never been given a durable home of its own — fix #2 above is the narrowest version that would have caught two of its four most recent instances.

### Dispositions

Thirteen items walked interactively; **all thirteen fixed on the branch**, none posted and none dropped, as in rounds 1-3. Fixtures 37 → 46; `Source/LinqToDB` Release census re-measured at exit 0 / 0 warnings / 0 errors. `P9`'s `G-01` and `TO-6` rows and a new `P11` `A-2` were written *before* the fix commit, per round 2's rule.

Every change carries its own proof, and per round 4's recommendation they are recorded as mutations with observed reds rather than as a narrative: the companion-type drift report reverted to a silent `return` (1 red), `count != 1` weakened to `count == 0` (1), the second brace-spelling flatten arm removed (1), `ClimbNegations`' parenthesis arm removed (1, with id *and* span inverted), `ReadSymbol`'s local arm removed (1), and the two model-reader guards defeated together (2, one each). Each run produced exactly its expected red and no other.

Two process notes worth keeping, both cases of this ledger's own rules catching the reviewer rather than the author:

- **A control refuted the round-4 write-up before it shipped.** OOS-1 was sharpened during the walk to claim an impossible *pair* inside an opaque call was also invisible. Run against the unwidened analyzer, the pair fixture was already **green**: Roslyn lowers the short-circuit into a flow capture landing in `block.Operations`, which `b394d942d` already walks. The real gap was only a single atom in a position with no flow capture of its own, and the #5727 shape wrapped in a helper was never invisible. The pair fixture was kept as the control that shows the widening changed only what was broken.
- **A reachability claim made in the same walk was also wrong, in the safe direction.** OOS-2(a) was presented as unreachable "by the type system", which held only for the accumulator positions; `Enum.HasFlag` takes `Enum`, so `flags.HasFlag(BuildFlags.ForSetProjection)` compiles and that member name is declared by both enums. The guard was therefore worth having, and it has a real fixture. A first attempt at that fixture used `BuildFlags.None`, whose value is 0, and drew a third diagnostic nobody predicted — `HasFlag(0)` genuinely is always true — which is what identified the free-modifier mask as the discriminating choice.
