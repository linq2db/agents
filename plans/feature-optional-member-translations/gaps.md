# Gap ledger: feature-optional-member-translations

## Round 1 — reviewed HEAD f1f6862c55 (linq2db#5926, 2026-09-14)

Findings as posted in the pending draft review. MIN013 and MIN014 were promoted from out-of-scope observations after the attribution was dispatched; their rows are attributed separately below the first seventeen.

### Attribution

- **MAJ001** — GAP-02 — P4 U-11 asked whether an opted-in BCL registration can throw client-side, but its census covered `StringMemberTranslatorBase` alone (both cases it found are E-4 exclusions). E-5 then admitted `:41-337` on the unchecked claim "every registration body there is `System.Convert`", and P12's closing paragraph recorded that E-5..E-8 never had a delegate-level census. Second (GAP-07): P12 parked that open risk on TO-6, a correctness-only run over existing tests, instead of an obligation that could see it (work-plan.md:140, `ad94484`, 2026-09-06). TO-6 could not go red here because no test calls these overloads; the only `Convert.ToDateTime(` under `Tests/` takes a string (`ConvertTests.cs:480`) — — — preventable: yes
- **MAJ002** — GAP-03 — P7 swept the legacy MapMember table in `Linq/Expressions.cs` only for its `Math.*` rows (`:611-643`, `:626`) and the `:568` route onto `Guid.ToString()`. No row asked which pre-gate rewrites land on an E-4 registration, although U-10 had already shown these rewrites run at `ConvertSingleExpression:868`, before the gate. `Expressions.cs:539,542-547` rewrite `string.CompareOrdinal` and `string.Compare` into `s1.CompareTo(s2)` — — — preventable: yes
- **MIN001** — GAP-10 — prose. The plan described both mechanisms correctly: U-2/D-4 say the mandatory registration pins the function, and D-5(a)/E-10 say `BuildFlags.InsideTranslation` guards the argument's translation. The E-11 comment at `Sql.cs:94-96` gives the first one's credit for the second one's job. No plan block was missing — — — preventable: no
- **MIN002** — not-a-gap: reviewer-disagrees-with-P10 — P10 #7 says "Data-dependent client-side exceptions are accepted as ordinary .NET semantics", with examples `"".Replace("", …)`, `Convert.ToInt32(badString)` and `PadLeft(negative)`, "exceptions included". The finding says a NULL receiver is the normal SQL value, not a malformed argument; the MapMember siblings route to null-safe `Sql.*` bodies (`Sql.PadRight`, `Expressions.cs:529-530`); and 6.5.0 translated these server-side. If the user rules that P10 #7 does not cover NULL receivers, re-attribute as GAP-01 (neither SC-6 nor P10 #7 names NULL-receiver behaviour), with a GAP-03 note that P7 has no row for the string MapMember siblings
- **MIN003** — GAP-07 — P8 gave E-7's 17 opted-in registrations no obligation that could fail. The only one was TO-6, a correctness-only `control` over a set it never expanded. Three rules already in force would each have forced a real obligation: work-plan.md:116 (expand a set obligation per element, `8bcd684`), :122 (each element's input must tell the two option arms apart, `6a7f503`) and :138 (a control records the mutation that turned it red, `a3561cf`). `work-plan-template.md:62` carries none of them, and it carries the only two P8 rules this plan applies. The unplanned batch test's own comment notes the option-off arm does not translate; the author then narrowed the assertion to `if (preferClient)` instead of treating the test as unable to fail — G-01 — preventable: yes
- **MIN004** — GAP-07 — same P8 shape for E-6's 12 `Round` registrations: TO-6 is their only obligation, and work-plan.md:116, :122 and :138 were not in the template. The plan already knew this decline shape, since critic W-2 found `TranslationFlags.Expression` making `ConvertToString:348` return null. No P4 row checked E-6's delegates for it (`MathMemberTranslatorBase.cs:232`) — G-01 — preventable: yes
- **MIN005** — GAP-10 — test craft. The fixture's comment sets the standard ("none of these may be pulled client-side"), but `Columns.Any(non-field)` is satisfied by `Sql.Concat` alone. `Sql.DateAdd` and `Sql.DatePart` are not TO-4 pins, so no plan block covered them. The `Sql.DateAdd` half has the same problem as MIN003: its constant input cannot tell the arms apart — — — preventable: no
- **MIN006** — GAP-07 — D-2 lists things no TO-n covers: its implementation note from critic O-8 (write the optional mark after the overwrite, clear it on re-registration outside a scope; the code at `TranslationRegistration.cs:84-89`) and five registration kinds that ignore the scope. TO-2 covers only one `RegisterMember` case. work-plan.md:118 (`348a372`, 2026-09-06) makes such a list an obligation checklist, and the template does not carry that rule. Second (GAP-08): TO-2 itself was never implemented, and G-08, which names TO-2, is still `(pending)` — G-08 — preventable: yes
- **MIN007** — GAP-08 — TO-3 included the CTE case (U-12's `ResetPrevious` path). It was the plan's only evidence that E-10(b) does anything, since critic O-4 marked the path unprobed. G-01 ("TO-3's reds must be observed") and G-08 both name TO-3, and both are still `(pending)`. The PR body's "78/78", plus a mutation that reddened "exactly the three nested tests", is the run-total shape work-plan.md:116 and :170 say cannot distinguish a missing element (one of TO-3's four) from a covered one. `work-plan.ps1:337` treats `(pending)` as a warning only, so `-Validate` never objected — G-01, G-08 — preventable: yes
- **MIN008** — GAP-08 — TO-4 listed eight pins and TO-7 two. The test file has three TO-4 pins and no TO-7 test, and G-01 and G-08 (which names TO-4 and TO-7) were never recorded. Residue in P8 itself (GAP-05): `Guid.CreateVersion7` is named in U-6 but not in TO-4. TO-1b still expects `string.Concat(new[]{…})` to move client-side ("same assertions"), even though revision 3's U-5/E-4 kept every sequence overload in SQL, so the array form had no correct obligation either way — G-01, G-08 — preventable: yes
- **MIN009** — GAP-07 — E-5's `Convert.ToString` registrations, E-7's `DateTimeOffset`/`DateOnly` registrations and E-8 map to no obligation except TO-6. P7's own `Convert.ToString` row says opting them in "is a server→client change and needs test coverage". work-plan.md:116 (expand a set obligation) and :140 (a risk P12 names becomes a P8 row before the verdict, `ad94484`) were in force but not in the template. Provider half (GAP-05): TO-6 names no provider set, and no obligation covers LinqService (U-3 was resolved by reading code, not by a test). The rule that asks what the test runner needs, work-plan.md:114, came from this branch's own TO-6 run and postdates the plan — G-01 — preventable: partly
- **MIN010** — GAP-10 — prose. E-12 authorized naming interpolation, and E-4 with U-5/U-5b already lists `string.Join` and the sequence `string.Concat` overloads as staying in SQL. The doc's "string concatenation" goes further than the plan's own exclusions. TO-1b's stale array-form expectation (see MIN008) is the only plan text that agrees with the doc, and no plan text says `string.Join` moves — — — preventable: no
- **MIN011** — GAP-03 — E-12 listed the option's doc sites (`LinqOptions.cs`, `DataOptionsExtensions.cs` ×2) without recording a search, and P7 has no row for the option's documentation. A `PreferClientCalculation` grep also finds `Common/Configuration.cs:465-477` — — — preventable: yes
- **MIN012** — GAP-10 — prose. Revision 3 rewrote SC-6 after critic O-5 specifically to drop "or changes value", and P10 #2 accepts formatting and rounding differences. The comment at `ConvertMemberTranslatorDefault.cs:18-21` and the `OptionalScope()` remarks bring back the revision-2 criterion that was dropped. The plan had the corrected wording — — — preventable: no
- **SUG001** — GAP-01 — P3 and D-1's failure mode both consider #5347 implementors (a translator not deriving from `MemberTranslatorBase` ignores the flag). But no SC-n requires the public `SkipOptional` doc to explain that to them, and E-1 says only "with an XML doc" — — — preventable: partly
- **NIT001** — GAP-10 — prose. D-2 justifies the closed set by keeping the ungated `VisitNew:948` and `HandleMember:1658` unreachable, and P7's call-site row records that `VisitUnary:2346` and `VisitBinary:3039` check the option before `TranslateMember`. The remarks apply the member/constructor reasoning to binary and unary kinds as well — — — preventable: no
- **NIT002** — GAP-02 — the comment at `StringMemberTranslatorBase.cs:38-39` copies P4 U-11 almost word for word ("i.e. exactly when the compiler picked that overload"). That claim about .NET behaviour was accepted from reading code and never tested with a probe, then carried into a code comment, which is the spread work-plan.md:166 (`6a7f503`) warns about — — — preventable: partly

### Aggregate

GAP-10 is the largest single class (5 of 16 attributed findings), but every one is a prose or assertion-strength slip, and in each case the plan had the correct fact (D-5, SC-6 after O-5, E-4's exclusions). So they do not suggest the design pass is failing to pay for itself. The main fixable cause is that rules written for exactly these shapes existed and never reached this plan. 6 findings (GAP-07 ×4, GAP-08 ×2, plus the second halves of MAJ001 and MIN006) trace to work-plan.md:116-170, which landed 2026-09-03 to 09-07, a week before the plan commit `5f9612f`. But `work-plan-template.md:62` carries only the proof modes and the symmetry-guard rule, and `work-plan.ps1:337` treats `(pending)` as a warning. As a result all nine gates stayed blank through review, TO-2, TO-3's CTE case, five TO-4 pins and TO-7 were never implemented, and TO-6 alone covered E-5..E-8. The single change that would have prevented the most findings (MIN003, MIN004, MIN006, MIN009) is putting those P8 rules into the fill comment the author writes against; the two majors are gaps the plan could see itself (a census P12 admitted was never run, and a MapMember table swept for `Math.*` but not `string.*`).

### Recommended durable fixes

- GAP-07 × 4 → an instruction doc: `work-plan-template.md:62`, the P8 fill comment — add one-line forms of work-plan.md:116, :118, :122, :138 and :140. Those rules already have worked examples; they failed only because they are absent from the text the author fills in.
- GAP-08 × 2 → a `definition-of-done.md` gate, enforced at `work-plan.ps1:337` — once any E-n file has a commit, `(pending)` is an error (work-plan.md:160 already says so), and G-09 is not dispatched until G-01 has one row per TO-n naming its test (:170).
- GAP-02 × 1, GAP-03 × 1 → the scout brief in `work-plan/SKILL.md` step 5 — for an edit set keyed by registered member, sweep the pre-gate rewrites that land on each key (MapMember targets in `Linq/Expressions.cs`, `[ExpressionMethod]` bodies), and resolve any P4 question across every E-n it covers, not just the file its census searched. The semantic-key rule at SKILL.md:93, added ten minutes after this plan, only covers which delegate each registration reaches.

### Promoted after dispatch

- **MIN013** — GAP-06 — D-4 moved `TranslateMember` ahead of the option check for every call and applied the option only to `HandleExtension`, so an attributed call now stays client-side only if `TranslateMember` returns nothing for it. P7's `GetAlreadyTranslated` row had already established that the cache returns a translation "before the translator can decline", and its own search (`GetAlreadyTranslated|RegisterTranslatedSql`) hits both the writer at `:1326` and the reader at `:5697`. SKILL.md step 6 tells the author to check a decision against the plan's own impact map for exactly this ("a decision that gates on the absence of a state your own impact map proved is present"), and plan-critic vector 8 carries the same check (both `cbfd3b5`, 2026-08-31); still, P10 #4 applied that row only to registry methods. Critic round 3 re-checked the risk D-4 stated ("no in-tree member is both attributed and registered"), although vector 8 (`348a372`, 2026-09-06) says to attack the risks a decision does not state. Not out-of-plan-scope: the finding anchors on D-4's call reorder (edit-point E-9), not on the deferred cache row. GAP-02 shape noted: D-4's failure-mode line assumed the reorder changes routing only for members that are both attributed and registered — — — preventable: yes
- **MIN014** — GAP-01 — SC-3 requires NULL to survive a missed LEFT JOIN only for `Sql.ToNullable`/`Sql.AsNullable`. SC-6 (the rule for which registrations may be optional) and P10 #2 and #7 never say what an optional registration computes from a NULL that is read as default(T). So nothing required E-5's `Convert.ToString`, E-8's `Guid.ToString`, E-7's `AddXxx` registrations, or E-12's list of documented consequences to handle it. The rule that should exist: the criterion for making a registration optional states its NULL-input behaviour, separately from value and exception differences. Not GAP-10, because the information predates the plan: U-9/D-5 show `Math.Abs(j.Value1)` reading 0 instead of null for a missed row, and #5923 (created 2026-09-14 11:15 UTC, four hours before plan commit `5f9612f`) is the binary-expression case D-5 absorbed — — — preventable: yes

Effect on the aggregate above: the GAP-07/GAP-08 cause becomes 6 of 18 attributed findings and the conclusion stands. Class totals: GAP-10 ×5, GAP-07 ×4, GAP-01 ×2, GAP-02 ×2, GAP-03 ×2, GAP-08 ×2, GAP-06 ×1. Both new rows are gaps the plan could see — P7's own cache search hit `:1326`, and U-9 measured the default(T) read. The recommended durable fixes are unchanged: each new row is a one-finding shape. MIN013 adds D-4 as a second worked example for rules that already exist (SKILL.md step 6, plan-critic vector 8); MIN014 is a one-off missing requirement.
