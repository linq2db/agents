<Project Sdk="Microsoft.Build.NoTargets/3.7.0">
	<!--
	Solution-visibility project only: no code, nothing to compile or pack.
	Globs the .claude/ submodule (github.com/linq2db/agents) so its content
	shows up in Solution Explorer without maintaining a per-file list here -
	new files in the submodule just appear on the next reload.
	-->
	<PropertyGroup>
		<TargetFrameworks />
		<TargetFramework>net10.0</TargetFramework>
		<IsPackable>false</IsPackable>
	</PropertyGroup>

	<ItemGroup>
		<!--drop everything Directory.Build.props injects (analyzers, polyfills, SourceLink, the Source/Shared Compatibility linkbase) - none of it applies to a docs-only glue project-->
		<PackageReference Remove="@(PackageReference)" />
		<Compile Remove="@(Compile)" />
	</ItemGroup>
</Project>
